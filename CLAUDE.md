# BVF — Claude Code 项目规则

## 三方分工

| 角色 | 职责 | 不能做的事 |
|------|------|-----------|
| **用户** | 最终视觉判断、发布决策、风格方向、止损 | — |
| **GPT** | 阶段规划、写指令、**第一道视觉审核**、比较版本、拦截明显不合格结果 | 最终发布决定、最终止损 |
| **Claude Code** | 执行构建、自动化验证、定位技术根因 | 判断"好不好看"、替 GPT 或用户说"通过"、自行扩大任务范围 |

GPT 有视觉能力，每轮渲染后 GPT 先看，过滤掉明显不合格的结果再交给用户最终裁定。

## GPT 与 Claude Code 的协作接口

GPT 和 Claude Code 不直接通信。用户负责在两者之间转发渲染文件、技术报告、视觉审核意见和下一轮指令。

Claude Code 每轮输出后必须停止。未收到用户转发的明确放行指令前，禁止假定 GPT 已完成审核，禁止自动进入下一阶段。

GPT 的视觉审核用于指出：

1. 渲染图中可见的明显问题。
2. 版本之间的视觉差异。
3. 可能的技术原因。
4. 下一轮建议优先处理的一个变量。

GPT 提出的技术原因属于待验证假设。Claude Code 必须使用结构化数据、诊断 Skill 和实际运行结果进行验证，不能直接把视觉推测当作已确认根因。

当 GPT 指出多个问题时，Claude Code 只处理用户明确批准的一个主要变量。其余问题记录到"新发现"，禁止顺手修改。

修改后必须通过 verify 或对应验证脚本报告：

1. 实际修改了哪些对象和参数。
2. 修改前后的量化值。
3. 原验证条件是否通过。
4. 是否出现新的回归问题。

禁止仅输出"已按 GPT 建议修改"。

## 每轮输出格式

Claude Code 每轮结束必须输出结构化摘要，只包含：

```markdown
## 本轮结果
- 通过: [验证条件 + 实测值]
- 未通过: [验证条件 + 实测值]
- 新发现: [可能影响后续轮次的问题]
- 建议下一轮优先解决: [1 个变量，不超过 1 个]
```

不输出"总结"、"已完成"、"下一步建议"等无量化数据的段落。

## 硬性流程规则

### 1. 新资产必须先孤立验证

任何新资产包导入后：
```
单角色导入 → 提取层级/骨架/bbox → 验证站立/朝向/比例 → 渲染三视图 → 通过 → 入库
```
全部角色通过前，禁止搭场景。

### 2. 渲染异常时排查顺序

```
角色层级 → 姿势 → Root 旋转 → 尺寸 → 地面接触 → 世界位置 → bbox → 相机
```
前六项未通过时禁止调 `ortho_scale`、`shift_y` 或 `camera distance`。

### 3. 返工限制

- 首帧阶段最多 2 轮主要返工
- 超过限制触发止损，等待用户决定

### 4. 版本管理

- 构建脚本和输出文件使用递增版本号
- 旧版本不覆盖，标记失败原因
- 所有需要用户上传给 GPT 的文件统一放入 `reviews/UPLOAD_NEXT/`

## Blender 5.1 已知兼容问题

- `Material.use_nodes` 是弃用属性（6.0 移除），脚本中使用不影响功能但会产生 DeprecationWarning
- Principled BSDF 输入必须使用名称而非索引：`inputs["Base Color"]` 而非 `inputs[0]`
- `fcurves` 访问需通过 `animation_data.action.fcurves`
- Eevee `use_gtao` 已移除，`use_soft_shadows` → `use_shadows`
- Keyframe 不支持跨 ID Block 路径（如 `node_tree.nodes["..."].inputs[0]`），用 visibility toggle 替代材质值动画
