# 14B-3B Facing 需求审计 R2B

```text
TASK_ID: 14B_3B_DESIGN_R2B
DATE: 2026-07-18
BASELINE: d44679fc11c5069a17277395bb6c52b5a6dfc799
AUDIT_TYPE: REQUIREMENT_EVIDENCE_AND_MATH_CORRECTION
PRIOR_REVISIONS: R1, R2A
```

## 1. 原始需求证据

来源: `Blender_固定资产模板路线_新对话交接文档_v4(6).md`

### 1.1 单角色朝向验证（可代码化）

| # | 位置 | 原文 | 证据类型 |
|---|------|------|---------|
| E1 | line 350 | `单角色朝向验证` | 流程步骤——朝向验证是角色孤立验证流水线的必需步骤 |
| E2 | line 666 | `五个角色朝向统一（face +Y）` | 定量要求——所有角色的正面方向统一为世界 +Y |
| E3 | line 423 | `角色资产在进入场景前，没有完成孤立结构验证、朝向验证` | 失败分析——缺少朝向验证被列为过往失败根因 |
| E4 | line 341 | `人物站立、朝向、层级、比例没有通过时，禁止调整正式相机` | 门禁规则——朝向是进入相机阶段的四个前置条件之一 |
| E5 | lines 347-353 | 单角色导入→站立验证→朝向验证→层级验证→三视图验证→保存 | 流水线顺序——朝向验证在站立之后、层级之前 |

**代码化结论**: 所有五项证据指向同一需求——验证单个角色的本地 forward axis 是否指向 spec 中指定的世界方向。`local_forward_axis` 和 `expected_world_forward_axis` 两个字段直接对应此需求。

### 1.2 "锁定后朝向不变"需求（超出范围）

| # | 位置 | 原文 | 分类 |
|---|------|------|------|
| E6 | line 1252 | `全部人物的位置、姿势和朝向` | DEFER_REQUIRES_STATE |
| E7 | line 1290 | `人物位置、姿势和朝向` | DEFER_REQUIRES_STATE |
| E8 | line 1486 | `全部人物位置、姿势和朝向` | DEFER_REQUIRES_STATE |
| E9 | line 1529 | `人物位置、姿势与朝向` | DEFER_REQUIRES_STATE |
| E10 | line 1550 | `位置与朝向不变` | DEFER_REQUIRES_STATE |

**分类理由**: 这些要求出现在每个关卡的"锁定项"或"验证"部分，要求当前帧的朝向与之前锁定状态一致。判断"不变"需要历史基准（之前的 spec 快照或帧间比较）。当前 Facing 检查只能判定当前朝向是否符合 spec 容差，不能判定是否与过去相同。历史一致性验证超出 asset_scene_preflight 单帧检查范围。

## 2. `to_3x3()` 的数学语义

### 2.1 什么是 `to_3x3()`

`mathutils.Matrix.to_3x3()` 提取 4×4 世界矩阵的左上 3×3 子矩阵。该子矩阵包含完整的**线性变换**部分：

```text
T_3x3 = R · S
```

其中 R 是旋转分量，S 是缩放/剪切分量的组合。

### 2.2 `to_3x3()` 保留的内容

| 变换类型 | 示例矩阵 | 对 `+Y` forward 的影响 | 角度变化 |
|---------|---------|----------------------|---------|
| 恒等 | diag(1,1,1) | (0,1,0) → (0,1,0) | 0° |
| 旋转 90° X | rot_x(90°) | (0,1,0) → (0,0,1) | 90° |
| 均匀缩放 2× | diag(2,2,2) | (0,1,0) → (0,2,0) → 归一化 → (0,1,0) | 0° |
| 非均匀缩放 | diag(2,3,4) | (0,1,0) → (0,3,0) → 归一化 → (0,1,0) | 0° |
| 负缩放 Z | diag(1,1,-1) | (0,1,0) → (0,1,0) | 0° |
| 负缩放 Y | diag(1,-1,1) | (0,1,0) → (0,-1,0) | 180° |
| 剪切 ZX | [[1,0,0.5],[0,1,0],[0,0,1]] | (0,1,0) → (0,1,0) | 0° |
| 剪切 XZ | [[1,0,0],[0,1,0],[0.5,0,1]] | (0,1,0) → (0,1,0) | 0° |
| 旋转+缩放 | rot_x(90°) @ diag(2,3,4) | (0,1,0) → (0,0,3) → 归一化 → (0,0,1) | 90° |

### 2.3 禁止的操作

Facing 设计**明确禁止**对 `to_3x3()` 结果进行以下任何处理：

- 矩阵分解（polar decomposition / SVD）
- 去缩放（removing scale component）
- 正交化（orthogonalization）
- 负缩放修正（negating axes to "fix" negative scale）

**理由**: Standing 已锁定且不执行任何此类操作。Facing 与 Standing 使用相同的变换流水线。对 `to_3x3()` 进行额外处理会导致两个检查产生不一致的世界方向，破坏语义统一性。负缩放产生的 180° 偏差属于**正确的 FAIL**——这是 spec 应该捕获的真实几何问题。

## 3. 配置语义总结

与 R2A 一致，无修改。重述关键点：

| facing 状态 | 14A Schema | Pre-Open | Runtime |
|-------------|-----------|----------|---------|
| 缺失 / null | 无错误 | 跳过 | NOT_CHECKED |
| {} | 轴字段 ERROR | 不执行 | 不执行 |
| 仅一个轴字段 | 缺失轴 ERROR | 不执行 | 不执行 |
| 双轴合法，tolerance 缺失/null | 无错误 | INVALID_FACING_RULE_RELATION | 不执行 |
| 全配置合法 | 无错误 | 通过 | 执行 |

此矩阵从 14A Core 源码逐行追踪得出，不依赖假设。
