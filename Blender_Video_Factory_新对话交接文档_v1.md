# Blender Video Factory 新对话交接文档 v1

> 交接日期：2026-07-14  
> 用途：供新的 ChatGPT 对话与 Claude Code 会话直接接手 Blender 视频路线。  
> 当前阶段：Remotion 第一条发布级基准视频尚未完成，主路线暂停；正式开始 Blender 独立验证。  
> 新路线定位：用 Blender 制作适合抖音的风格化三维或 2.5D“生活机制模拟短片”。  
> 首个验证目标：先证明 Blender 能否以可接受的制作成本，做出比纯 Remotion 更有空间感、动作感和抖音观看价值的短片。

---

## 一、这次交接的核心结论

项目需要切换主视觉工具。

Remotion 本身是优秀的程序化视频工具，已经证明可以完成：

- 参数化 UI 动画
- 精确计时和数字变化
- 分 Scene 渲染
- 视觉审核
- 局部返工
- 外部音频和 SRT 导入
- 自动化生产流程

但第一条正式基准视频经过多轮人工返工后，暴露出明显的结构性问题：

1. 纯 UI、卡片、计时器和信息图画面容易接近产品演示、工程 Demo 或 PPT。
2. 画面缺少人物、空间、物体运动、真实动作和情绪关系。
3. 每个 Scene 都需要从零完成视觉设计，Claude Code 很难稳定承担成熟 Motion Designer 的职责。
4. 坐标、缩放、字号、共享元素转场等工程问题占用了大量返工时间。
5. 单个 Scene 可以逐步修好，整条视频仍然可能缺乏抖音需要的第一眼冲击、情绪和平台原生感。
6. 继续更换另一个纯动态图形工具，可能仍会重复“画面精确但不像抖音内容”的问题。

因此，新路线不再让 Remotion 承担整条视频的主要视觉。

新的主工具确定为：

```text
Blender
```

新的最终剪辑工具确定为：

```text
剪映专业版
```

Remotion 仓库保留，未来只作为局部 UI、数据、数字、字幕和参数化片段工具。

---

## 二、固定决策

以下决策已经由用户确认，后续新对话不要再次反复讨论。

### 1. 新建独立仓库

新仓库建议路径：

```text
D:\blender-video-factory
```

原仓库继续保留：

```text
D:\video-factory
```

两个仓库职责分开：

| 仓库 | 定位 |
|---|---|
| `D:\blender-video-factory` | Blender 主视觉验证与后续三维视频生产 |
| `D:\video-factory` | Remotion 历史项目、局部 UI/数据动画、审核工具与外部音频/SRT能力 |

不得直接把原 Remotion 仓库重构成 Blender 工程。

### 2. 首个 Blender 验证片

题目固定为：

> 一个收银窗口关闭后，队伍为什么突然变长？

首片只用于验证 Blender 路线，不直接建设完整视频工厂。

目标时长：

```text
10 至 12 秒
```

目标平台：

```text
抖音
```

画面比例：

```text
1080×1920
```

帧率：

```text
30fps
```

首片先做无配音视觉验证。视觉通过后，再进入剪映添加音效、字幕或旁白。

### 3. 首阶段禁止复杂人物动画

首阶段人物只允许：

- 静止站立
- 整体平移
- 简单排队移动
- 简单转向
- 必要时使用非常基础的走路循环

首阶段禁止：

- 人物对话
- 口型
- 面部表情
- 写实人脸
- 复杂手部动作
- 拿取物品
- 两人接触
- 布料和头发模拟
- 多人自然表演
- 复杂骨骼绑定
- 复杂角色动画

首片中的顾客可以先使用低多边形“棋子式人物”、几何人物或简化人形。人物主要作为空间流量单位，不承担情绪表演。

### 4. 最终剪辑使用剪映专业版

Blender 负责：

- 三维场景
- 物体和人物移动
- 摄像机
- 灯光
- 材质
- 镜头输出

剪映专业版负责：

- 镜头拼接
- 配音
- 字幕
- 音效
- 音乐
- 节奏微调
- 最终 H.264 竖屏成片

第一条 Blender 验证片不要求自动生成剪映工程。

---

## 三、Blender 新路线的内容定位

新方向定位于轻量、可复用的风格化三维机制短片，避开电影级三维和复杂角色短剧。

正式定位为：

# 风格化三维或 2.5D 生活机制模拟短片

其核心特点：

1. 使用一个明确的生活空间。
2. 使用简化人物、车辆、商品、道路、窗口等可复用资产。
3. 通过数量、位置、速度、距离和空间拥堵展示隐藏机制。
4. 第一秒就出现可识别的对象和异常变化。
5. 旁白负责提出问题和补充判断，画面负责直接展示机制。
6. 单条视频控制在 15 至 35 秒。
7. 每条只解释一个机制。
8. 每个镜头只表现一个主要变化。
9. 不追求写实，追求清楚、统一、有空间感和动作感。

如果路线验证成功，优先制作以下内容：

- 排队和服务瓶颈
- 道路拥堵和车流传播
- 集中需求导致货架清空
- 订单如何被司机筛选
- 电梯多次停靠为什么变慢
- 多个小额支出怎样累积
- 任务或物品怎样不断占满空间
- 供需、库存、距离和等待的动态关系
- 人流、物流和信息流的空间机制

初期不优先制作：

- 人物情绪剧情
- 对话短剧
- 写实历史还原
- 古代场景
- 战争和灾难大场面
- 写实城市
- 复杂手机操作
- 纯软件教程
- 需要持续面部表演的内容

---

## 四、Remotion 路线当前状态

原正式仓库：

```text
D:\video-factory
```

Video Factory Phase 1 已验收通过，基础设施能够正常工作。

已经具备：

- Remotion 渲染
- PhoneMockup
- CursorAnimation
- LottieGesture
- 安全区和字幕组件
- 分 Scene Composition
- FFmpeg 抽帧与 contact sheet
- DashScope qwen3.7-plus 视觉审核
- JSON 验证
- Scene 状态机
- 外部 WAV/MP3 导入
- SRT 导入
- Claude Code 项目级 Skills

第一条正式基准视频：

```text
vf_benchmark_001_fragmented_rest
```

题目：

> 成年人的休息，为什么总被消息切碎？

当前并未完成最终发布级验收。

最后一次人工状态大致为：

| 项目 | 状态 |
|---|---|
| Scene 1 | 主体视觉接近通过，仍有轻微点击位置问题 |
| Scene 2 | 尚未按新尺度完成最终版本 |
| Scene 3 | 仍有构图和缩放问题 |
| Scene 4 | 尚未完成最终重构 |
| Scene 5 | 主体机制画面已基本通过 |
| Scene 6 | 面板尺寸、转场和操作仍有问题 |
| 完整 24 秒视频 | 尚未完成最终版 |
| 配音和字幕 | 尚未导入 |

该项目暂停继续大规模返工。

禁止删除以下内容：

- 原仓库
- 原项目代码
- v1 至 v4 渲染结果
- contact sheet
- 审核 JSON
- direction.json
- script.json
- scenes.json
- 人工验收记录

原项目的定位从“当前主线”改为：

```text
历史技术验证 + 局部 Remotion 资产来源
```

未来可保留使用的能力：

- 精确数字动画
- 时间和价格变化
- 手机 UI
- 参数化字幕
- 简单数据片段
- 封面和多版本输出
- Blender 成片上方的少量二维信息覆盖

---

## 五、首个 Blender 验证片的核心机制

题目：

> 一个收银窗口关闭后，队伍为什么突然变长？

核心判断：

> 当服务窗口从三个减少为两个时，原来分散在三个入口的顾客会重新汇集到剩余窗口。新增等待不只来自关闭窗口前的几个人，还来自后续顾客持续进入，所以队伍会在短时间内明显拉长。

首片不需要完整讲出以上文字。

视觉上只需证明：

```text
3 个窗口正常服务
→ 1 个窗口关闭
→ 原队伍重新分流
→ 后续顾客继续进入
→ 剩余两条队伍明显变长
```

无声观看也必须理解：

```text
一个服务点关闭，导致剩余队伍快速拥堵。
```

---

## 六、首个验证片的镜头方案

目标总时长：

```text
10 至 12 秒
```

建议使用 4 个镜头或一个连续镜头中的 4 个明确阶段。

### 镜头 1：正常状态

时间建议：

```text
0.00 至 2.00s
```

画面：

- 竖屏微缩超市收银区。
- 三个收银窗口并排。
- 每个窗口前有 2 至 3 名简化顾客。
- 三条队伍长度大致相近。
- 顾客以稳定、缓慢节奏向前移动。
- 第一秒必须看懂“这里有三个收银队伍”。

摄像机：

- 斜俯视。
- 能同时看到三个窗口和三条短队伍。
- 主体占画面中部大部分区域。
- 不允许镜头过远。

### 镜头 2：窗口关闭

时间建议：

```text
2.00 至 4.00s
```

画面：

- 中间窗口的灯牌熄灭。
- 中间窗口出现清楚的关闭状态。
- 收银员可以消失、退后或窗口挡板落下。
- 中间队伍停止前进。
- 队伍中的顾客发生轻微停顿。

关闭状态必须通过实体动作表现，例如：

- 灯牌熄灭
- 挡板落下
- 柜台颜色变暗
- 关闭标识出现

不要只依赖小字说明。

### 镜头 3：重新分流

时间建议：

```text
4.00 至 7.50s
```

画面：

- 中间队伍的顾客开始向左右两条队伍移动。
- 顾客使用整体平移，不要求复杂走路和手脚动画。
- 左右队伍被插入新顾客。
- 后方仍有新顾客进入画面。
- 左右队伍长度迅速增加。
- 顾客之间保持合理间距，避免互相穿模。

摄像机：

- 可以轻微推近。
- 或从斜俯视过渡到更高俯视。
- 必须让“分流”方向清楚。

### 镜头 4：瓶颈结果

时间建议：

```text
7.50 至 11.50s
```

画面：

- 只剩两个窗口工作。
- 两条队伍明显变长。
- 中间窗口空置。
- 后方仍有顾客进入。
- 队伍前进速度变慢。
- 最终停留 0.8 至 1.2 秒，让结果被看清。

摄像机：

- 使用更高的俯视或轻微拉远。
- 同时看到关闭的中间窗口和两条长队。
- 画面必须在静音情况下完成因果闭环。

可选的极简文字只允许在后期使用：

```text
3 个窗口 → 2 个窗口
```

首轮 Blender 灰盒不依赖文字完成理解。

---

## 七、首片的视觉风格

视觉风格固定为：

```text
低多边形 + 微缩模型 + 简单材质 + 清晰灯光
```

### 人物

- 无五官或只有极简五官。
- 统一比例。
- 统一材质。
- 使用 3 至 5 种服装颜色变化。
- 不使用写实皮肤。
- 不使用复杂头发。
- 人物可以由简单几何体组成。
- 首片不要求骨骼动画。

### 场景

- 三个收银窗口。
- 简化传送带或柜台。
- 简化灯牌。
- 地面分区。
- 少量货架轮廓作为背景。
- 背景物体不抢主体。

### 色彩

建议只使用：

- 深色或中性背景
- 米白或浅暖主体
- 一种暖色强调
- 少量低饱和人物服装色

关闭窗口可以使用唯一强调色或明显降低亮度。

不得出现过多高饱和颜色。

### 灯光

使用固定三点式或简化工作室灯光：

- 主光
- 柔和补光
- 轮廓光或环境光

目标：

- 人物和柜台分离
- 队伍轮廓清楚
- 关闭窗口状态明显
- 阴影不过黑
- 场景不显得像默认灰盒

### 材质

- 以 Principled BSDF 简单材质为主。
- 控制粗糙度。
- 不使用复杂程序材质。
- 不使用写实玻璃。
- 不使用大量反射。
- 不使用高成本体积雾。

---

## 八、技术和渲染边界

用户电脑主要硬件：

```text
CPU：i5-13490F
GPU：AMD RX7600
RAM：32GB
Windows 10
```

Claude Code 首先必须审计实际 Blender 环境，不能假设版本和 GPU 后端。

### 首先需要确认

1. Blender 是否已安装。
2. Blender 完整版本号。
3. Blender 可执行文件路径。
4. Blender 内置 Python 版本。
5. 命令行启动是否可用。
6. `blender --background` 是否可用。
7. Eevee 是否可用。
8. GPU 渲染后端是否可用。
9. RX7600 是否可以在当前 Blender 版本中正常使用。
10. FFmpeg 是否可用。
11. Blender 是否可以输出 PNG 序列。
12. Blender 是否可以无界面批量渲染。
13. 当前安装是否存在第三方插件冲突。

不得在审计前直接下载大型资产库或安装大量插件。

### 默认渲染策略

灰盒和构图预览：

```text
540×960
30fps
Eevee
低采样
关闭高质量运动模糊
简化阴影
```

正式验证输出：

```text
1080×1920
30fps
Eevee
10 至 12 秒
PNG 图像序列优先
```

使用 PNG 序列的原因：

- 渲染失败后可以续渲。
- 单帧检查方便。
- 后续进入剪映或 FFmpeg 更稳定。
- 避免长时间渲染后因视频封装失败丢失全部结果。

正式 MP4 可以由 FFmpeg 或剪映生成。

Cycles 暂不作为默认渲染器。只有 Eevee 无法达到目标画面时再单独评估。

---

## 九、Blender Python 自动化原则

Claude Code 可以通过 Blender Python API 完成：

- 创建集合
- 创建基础几何体
- 导入资产
- 创建材质
- 放置人物
- 放置柜台
- 创建灯光
- 设置摄像机
- 设置关键帧
- 设置运动路径
- 设置渲染参数
- 执行预览渲染
- 执行图像序列渲染

首阶段不追求“一键输入选题自动生成视频”。

自动化目标仅为：

1. 场景可重复构建。
2. 参数可修改。
3. 人物数量和队伍长度可调整。
4. 镜头位置可配置。
5. 灯光和渲染预设可复用。
6. 修改后能够可靠重建和重渲。

所有关键参数应集中配置，例如：

```python
VIDEO_WIDTH = 1080
VIDEO_HEIGHT = 1920
FPS = 30
DURATION_SECONDS = 12

CHECKOUT_COUNT = 3
INITIAL_QUEUE_PER_LANE = 3
CLOSED_LANE_INDEX = 1
NEW_CUSTOMER_COUNT = 4
```

不要把所有坐标和材质散落在大量脚本中。

---

## 十、建议的仓库结构

```text
D:\blender-video-factory
│
├── README.md
├── CLAUDE.md
├── PRODUCTION.md
├── HANDOFF.md
├── requirements.txt
│
├── config/
│   ├── project_defaults.json
│   ├── render_preview.json
│   └── render_final.json
│
├── assets/
│   ├── characters/
│   ├── props/
│   ├── environments/
│   ├── materials/
│   └── licenses/
│
├── blender/
│   ├── templates/
│   │   └── base_vertical_scene.blend
│   ├── scripts/
│   │   ├── audit_environment.py
│   │   ├── build_scene.py
│   │   ├── build_assets.py
│   │   ├── build_lighting.py
│   │   ├── build_cameras.py
│   │   ├── animate_queue.py
│   │   ├── render_preview.py
│   │   └── render_final.py
│   └── modules/
│       ├── geometry.py
│       ├── materials.py
│       ├── camera.py
│       ├── lighting.py
│       ├── characters.py
│       └── paths.py
│
├── projects/
│   └── bvf_test_001_checkout_bottleneck/
│       ├── INPUT.yaml
│       ├── script.json
│       ├── direction.json
│       ├── shots.json
│       ├── scene.blend
│       ├── src/
│       ├── previews/
│       ├── frames/
│       ├── renders/
│       ├── reviews/
│       └── HUMAN_REVIEW.md
│
├── tools/
│   ├── extract_frames.py
│   ├── make_contact_sheet.py
│   ├── encode_preview.py
│   └── validate_project.py
│
└── docs/
    ├── ASSET_POLICY.md
    ├── BLENDER_STYLE_GUIDE.md
    └── TROUBLESHOOTING.md
```

首阶段不要为了结构完整创建大量空文件。

只创建当前验证片真正需要的目录和脚本。

---

## 十一、资产策略

首片优先使用 Blender 原生几何体自己创建：

- 人物
- 柜台
- 地面
- 灯牌
- 队列分隔
- 简化货架

原因：

- 无版权风险
- 风格容易统一
- 方便 Python 参数化
- 不依赖外部模型格式
- 不会出现素材拼装感

如果必须使用外部资产，必须记录：

- 来源
- 作者
- 许可证
- 是否允许商业使用
- 是否允许修改
- 是否需要署名
- 原始文件路径
- 修改后的文件路径

每个外部资产的许可证信息放入：

```text
assets/licenses/
```

禁止：

- 随意下载来源不明模型
- 使用无法确认商业许可的角色
- 把多个不同风格模型直接混用
- 在首片中引入大型资产包
- 为了“看起来丰富”堆满背景

---

## 十二、生产阶段

### 阶段 0：环境审计

输出：

- Blender 版本
- 可执行路径
- Python 版本
- 渲染后端
- GPU 状态
- FFmpeg 状态
- 命令行渲染测试
- 10 帧最小测试结果

未通过前不得进入正式建模。

### 阶段 1：机制和镜头锁定

输出：

- INPUT.yaml
- direction.json
- shots.json
- 每个镜头的主体、动作、结果
- 无声理解路径

不要开始高质量材质和灯光。

### 阶段 2：灰盒场景

使用：

- 纯色几何人物
- 简单柜台
- 简单地面
- 简单摄像机
- 无复杂材质

输出：

- 540×960 灰盒预览
- 每 0.25 秒关键帧
- contact sheet

人工先判断：

- 机制是否看懂
- 镜头是否清楚
- 队伍移动方向是否正确
- 竖屏主体是否足够大

### 阶段 3：动作修正

重点检查：

- 人物是否穿模
- 队伍是否重叠
- 分流是否自然
- 关闭窗口动作是否可见
- 后续顾客是否持续进入
- 镜头之间是否连续

此阶段仍不追求最终美术。

### 阶段 4：视觉风格

加入：

- 正式颜色
- 材质
- 灯光
- 阴影
- 背景层次
- 必要景深

输出：

- 540×960 风格预览
- 1080×1920 少量测试帧

### 阶段 5：最终 Blender 渲染

输出：

- PNG 序列
- 完整无声预览
- contact sheet
- 关键镜头单帧

### 阶段 6：剪映后期

加入：

- 音效
- 字幕
- 旁白
- 必要标题
- 最终节奏
- 导出 H.264 MP4

### 阶段 7：人工验收

最终是否通过由用户实际观看决定。

---

## 十三、质量验收标准

首个验证片必须同时满足以下标准。

### 静音理解

1. 第一秒能看出有三个收银窗口和三条队伍。
2. 两秒左右能看出中间窗口关闭。
3. 六秒前能看出顾客向剩余窗口分流。
4. 九秒前能看出剩余队伍明显变长。
5. 最终能理解“窗口减少导致队伍拥堵”。

### 抖音画面

1. 竖屏主体足够大。
2. 第一秒存在清楚空间和人物动作。
3. 每 1 至 2 秒出现有意义变化。
4. 不出现大面积无意义空白。
5. 不依赖小字说明。
6. 不像软件 Demo。
7. 不像三维灰盒作业。
8. 不像素材拼装。
9. 画面中始终有明确焦点。
10. 最终结果至少停留 0.8 秒。

### 三维技术

1. 人物不穿模。
2. 人物不漂浮。
3. 队伍间距合理。
4. 移动速度稳定。
5. 镜头不眩晕。
6. 物体比例统一。
7. 灯光不过暗。
8. 关闭窗口状态清楚。
9. 阴影和材质没有明显闪烁。
10. 输出无缺帧和黑帧。

---

## 十四、自动审核和人工审核

可以继续使用原有视觉审核思路，但需要调整预期。

视觉模型适合检查：

- 对象是否存在
- 人物是否重叠
- 窗口是否关闭
- 构图是否过空
- 画面是否过暗
- 文字是否裁切
- 风格是否一致

视觉模型不适合单独判断：

- 动作是否自然
- 队伍分流是否连贯
- 镜头节奏是否适合抖音
- 是否有情绪和观看吸引力
- 整条视频是否值得发布

每轮审核必须包含：

- 完整 MP4
- 普通 contact sheet
- 每 0.25 秒密集 contact sheet
- 关键动作前后帧
- 用户人工观看

不得仅凭自动评分宣布通过。

---

## 十五、状态机建议

首个 Blender 项目使用以下状态：

```text
planned
environment_checked
graybox
preview_rendered
reviewing
revise
visual_pass
final_rendered
post_produced
locked
```

状态解释：

| 状态 | 含义 |
|---|---|
| planned | 项目与镜头已规划 |
| environment_checked | 环境审计通过 |
| graybox | 灰盒场景已建立 |
| preview_rendered | 已渲染低分辨率预览 |
| reviewing | 正在人工或视觉审核 |
| revise | 需要返工 |
| visual_pass | Blender 视觉通过 |
| final_rendered | 正式图像序列已输出 |
| post_produced | 剪映后期完成 |
| locked | 最终人工锁定 |

---

## 十六、成本和复杂度控制

首个验证片的目的只有一个：

> 判断 Blender 是否能够以可接受的制作成本，产出明显优于纯 Remotion 的抖音视觉。

禁止把首片扩展成完整 Blender 工厂建设。

首片限制：

- 只做一个场景
- 只做三个收银窗口
- 人物不超过 12 至 15 个
- 不使用复杂骨骼
- 不使用写实材质
- 不使用 Cycles 作为默认
- 不使用复杂模拟
- 不使用大型城市
- 不使用第三方复杂插件
- 不自动生成长篇旁白
- 不制作完整频道包装
- 不一次性建立几十个资产

返工优先级：

1. 机制是否看懂
2. 主体尺度
3. 移动方向
4. 摄像机构图
5. 灯光和颜色
6. 材质细节

不得先花大量时间处理材质细节，再发现机制看不懂。

---

## 十七、止损标准

Blender 路线不能无限投入。

首个验证片完成后，需要与当前 Remotion 路线比较：

1. 第一秒哪个更有观看欲望。
2. 哪个更像抖音内容。
3. 哪个无声观看更容易理解。
4. 哪个更有空间感和动作感。
5. 哪个制作和返工成本更合理。
6. 哪个更适合后续复用。

如果经过灰盒和两轮主要返工后，仍然出现以下情况，应暂停 Blender 路线：

- 无法在静音情况下理解机制
- 画面仍像学校三维作业
- 资产和动作成本明显不可控
- 单条 10 秒测试仍需要大量复杂建模
- Claude Code 无法稳定重建场景
- 渲染速度无法接受
- 画面并未明显优于 Remotion
- 用户本人不愿意继续观看这种风格

暂停不代表删除仓库。需要保存审计、脚本、资产和渲染结果，再重新选择内容形态。

---

## 十八、Claude Code 首次任务

新 Claude Code 会话的工作目录：

```text
D:\blender-video-factory
```

首次任务只能进行环境审计和最小技术验证。

可以直接发送：

```text
请在 D:\blender-video-factory 开始 Blender Video Factory 的第一阶段。

先完整读取《Blender Video Factory 新对话交接文档 v1》。

本轮只执行环境审计和最小命令行渲染验证，不要开始正式建模，不要下载资产，不要安装大量插件，也不要设计完整生产系统。

请完成：

1. 确认 Blender 是否安装。
2. 输出 Blender 可执行文件完整路径。
3. 输出 Blender 版本。
4. 输出 Blender 内置 Python 版本。
5. 测试 blender --background 是否可用。
6. 检查 Eevee 是否可用。
7. 检查 AMD RX7600 在当前 Blender 中可用的渲染后端。
8. 输出当前 GPU 渲染设备检测结果。
9. 确认 FFmpeg 是否可用。
10. 新建最小测试目录，但不要建立大量空目录。
11. 使用 Blender Python 创建一个最小竖屏场景：
    - 540×960
    - 30fps
    - 10 帧
    - 一个立方体
    - 一个地面
    - 一个摄像机
    - 一个主光
    - Eevee
12. 使用命令行无界面渲染 10 帧 PNG。
13. 使用 FFmpeg 合成最小 MP4。
14. 输出全部执行命令、环境结果、渲染结果和发现的问题。

本轮禁止：

- 开始收银窗口正式项目
- 下载外部人物模型
- 安装 Blender 插件
- 使用 Cycles 做正式测试
- 建立完整 Agent 流水线
- 修改原 D:\video-factory 仓库
- 重新讨论 Remotion 是否优秀
- 在最小渲染未通过前继续下一阶段

完成后等待用户和 ChatGPT 审核。
```

---

## 十九、新 ChatGPT 对话的职责

新对话负责：

1. 阅读本交接文档。
2. 审核 Claude Code 的 Blender 环境报告。
3. 确认是否进入正式灰盒阶段。
4. 审查首片 INPUT.yaml。
5. 审查镜头和机制。
6. 观看 Blender 灰盒预览。
7. 逐轮给出局部返工指令。
8. 判断 Blender 路线是否值得继续。
9. 禁止仅凭截图或自动评分宣布成功。
10. 在 Blender 路线通过后，再设计后续资产沉淀和生产文档。

新对话不要：

- 再次完整审计原 Remotion GitHub 项目
- 删除原 Video Factory
- 一开始就建立大型 Blender 资产库
- 直接下载大量第三方模型
- 开始复杂角色动画
- 把首片变成正式长视频
- 先讨论几十条后续选题
- 在没看到真实 Blender 预览前不断增加规则
- 仅凭 Blender 可以编程就宣布路线可行

---

## 二十、新对话第一条建议回复

用户上传本交接文档后，新对话应回复：

```text
我已接手 Blender 新路线。原 Remotion 仓库保留为历史项目和局部工具，当前主线是独立仓库 D:\blender-video-factory。第一步只验证 Blender 环境、命令行 Python 和最小竖屏渲染，不开始正式场景。请把 Claude Code 的环境审计报告粘贴过来。
```

---

## 二十一、当前唯一下一步

当前唯一下一步是：

```text
在 D:\blender-video-factory 启动 Claude Code
→ 发送环境审计任务
→ 获取 Blender 版本、路径、GPU、Eevee、Python、FFmpeg 和最小渲染报告
→ 由新 ChatGPT 对话审核
```

在环境审计完成前：

- 不制作正式收银场景
- 不下载模型
- 不安装插件
- 不建立完整工厂
- 不继续返工原 Remotion 基准视频
