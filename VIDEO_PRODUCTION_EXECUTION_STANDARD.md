# Blender 视频工厂完整生产执行规范

```text
DOCUMENT_ID: VIDEO_PRODUCTION_EXECUTION_STANDARD
VERSION: R3
DATE: 2026-07-28
PROJECT_ROOT: D:\blender-video-factory
DOCUMENT_ROLE: Blender 视频生产流程最高级通用规范
APPLIES_TO: 本仓库内所有新建和继续推进的视频项目
STATUS: DRAFT_FOR_USER_REVIEW
```

## 一、文档目的

本规范回答一件事：

> 从一个视频选题开始，如何稳定地使用 GPT、Claude Code、blender-mcp、Blender、项目质检代码和人工视觉审核，最终生成可交付的视频。

本项目的最终目标是持续产出视频，不是持续扩建测试、证据、报告、架构或通用化系统。质检、脚本、MCP 和文档都是生产工具，不得反过来成为项目目标。

本规范只记录长期稳定的通用生产规则，不记录某一条视频的临时状态、具体资产路径、当前版本或下一轮参数。每条视频的实时状态必须记录在该项目自己的 `VIDEO_PLAN.md` 中。

## 二、权威边界与优先级

不同文件只在各自范围内具有权威性，不得互相替代。

1. 用户本轮明确指令：决定本轮授权、审美裁定、是否继续、止损与发布。
2. `VIDEO_PRODUCTION_EXECUTION_STANDARD.md`：决定所有视频共同遵守的生产顺序、阶段门槛、角色边界和任务设计原则。
3. 当前视频的 `VIDEO_PLAN.md`：决定该视频的选题、已锁定内容、当前阶段、当前版本、正式入口和唯一下一任务。
4. 根目录 `CLAUDE.md`：决定 Claude Code 的长期执行纪律、真实性、验证、交付、返工和停止规则。
5. `reviews/PROJECT_CODEIFICATION_MASTER_MAP.md`：只记录通用质检系统的实现和锁定状态，不决定某条视频当前该做什么。
6. 设计表、配置文件、资产清单和项目脚本：只在当前视频和当前阶段内生效。

发生跨范围冲突时，不得自行拼接解释。冲突会改变目标、范围、验收标准、允许修改内容或下一步时，必须停止并报告。

## 三、系统组成与职责

### 3.1 用户

用户保留以下最终权力：

1. 选题是否值得做。
2. 画面审美是否满意。
3. 是否继续投入、止损或发布。
4. 是否批准进入需要人工放行的下一阶段。

用户不负责选择 Blender API、脚本结构、测试写法、坐标计算、文件组织或技术排查方案。

### 3.2 GPT 主控

GPT 是视频项目的导演和技术主控，负责：

1. 把用户目标转化为分阶段生产计划。
2. 每轮先定义一个用户可以直接审核、能够推进最终视频的主要产出。
3. 冻结本轮唯一目标、权威输入、允许和禁止修改范围、验收标准、停止条件和交付物。
4. 决定技术路线、任务拆分、质检范围、修复顺序和技术止损。
5. 根据任务复杂度编写可直接发送给 Claude Code 的执行指令；简单任务不得机械扩写成大型规范。
6. 审核 Claude Code 的真实输出、代码、报告和交付文件。
7. 对图片和视频进行第一道视觉审核，拦截明显技术或视觉错误。
8. 视觉任务必须先查看实际 PNG 或 MP4，再读取 Claude Code 的完成报告和技术说明。
9. 视觉结果与文字汇报、文件统计或技术数据冲突时，以实际图片或视频为准。
10. 将只有审美才能决定的问题交给用户裁定。
11. 防止测试、证据、报告、打包和架构工作挤占生产。

GPT 的技术判断属于待验证结论。Claude Code 必须通过源码、结构化数据、Blender 实际运行或诊断工具确认。

GPT 不得把视觉批准权交给 Claude Code，也不得因为 Claude Code 报告 `COMPLETED`、`PASSED` 或某字段为 `TRUE`，就跳过对真实产物的独立审核。

### 3.3 Claude Code

Claude Code 是确定性施工与技术验证执行者，负责：

1. 读取本轮权威文件和明确任务。
2. 修改授权范围内的配置、Python 脚本和项目文件。
3. 通过 blender-mcp 或 Blender 后台进程执行 `bpy` 脚本。
4. 构建场景、设置动画、保存 `.blend`、渲染预览和运行必要质检。
5. 提取层级、坐标、旋转、骨骼、包围盒、可见性、动画和输出文件状态。
6. 在技术条件失败时停止并报告真实失败原因。
7. 生成当前任务要求的最小交付物并停止。
8. 只报告真实执行事实，包括使用的输入、完成的操作、生成的输出和仍存在的阻断。

`TASK_STATUS: COMPLETED` 只表示本轮明确要求的工作和交付已经完成，不代表 GPT 或用户已经完成视觉验收，也不自动批准进入下一阶段。

Claude Code 不得：

1. 自行决定画面是否好看、是否具有发布价值或是否通过视觉审核。
2. 自行批准资产路线、首帧、功能样片、完整预览或最终成片。
3. 自行开始下一阶段。
4. 绕过质检门禁直接交付。
5. 通过反复盲调参数寻找“满意效果”。
6. 同时修改多个独立基础变量。
7. 把测试通过、渲染成功、文件存在或报告字段完整等同于用户已经验收。
8. 为了让汇报字段变成 `TRUE` 而改变实现、放宽标准、替换验证对象、使用替代产物或隐瞒失败。

### 3.4 blender-mcp

`blender-mcp` 是外部 Blender 控制工具，不属于 `blender-video-factory` 仓库本体。它负责：

1. 查询 Blender 状态和环境。
2. 调用当前环境已经验证可用的 Blender 执行通道。
3. 执行内联或文件形式的 `bpy` 脚本。
4. 查询场景状态、保存场景和触发授权范围内的渲染。

复杂视频不得依靠数百次零散 MCP 操作逐个创建、移动和设置对象。默认方法是：

```text
Claude Code 生成确定性配置与 bpy 脚本
→ blender-mcp 或 Blender 后台进程执行脚本
→ Blender 完成实际场景构建
```

MCP 是“执行的手”，不是导演、动画设计师或质检员。工具名称可能随安装版本变化，执行时以当前 MCP 服务真实暴露的工具为准。

### 3.5 Blender

Blender 是实际生产环境，负责：

1. 资产导入与 Append。
2. 场景、材质、灯光、摄像机和动画计算。
3. `.blend` 保存。
4. PNG、帧序列和预览视频渲染。

默认优先使用可重复的后台执行。Live GUI 不是生产前提，也不能赋予 Claude Code可靠的视觉判断能力。只有当前环境单独验证通过并且本轮明确授权时，才用于用户观察或人工检查。

### 3.6 通用质检系统

`protocol_guard/phase3_min` 是通用场景与资产质检底座，负责客观、可重复判断的要求，例如：

1. 对象存在性和类型。
2. 层级。
3. 站立。
4. 朝向。
5. 可见性。
6. 旋转。
7. 采样帧中的 Action、可见性和已配置状态字段。
8. 材质分配。
9. Collection 规则。
10. 地面接触。
11. 已配置摄像机结构与条件检查。
12. 已配置投影组的包围盒、角点和屏幕范围检查。

这些检查只证明配置中声明且实际执行的客观条件。它们不证明：

1. 帧间运动连续。
2. 动作自然。
3. 整个时间轴始终无遮挡。
4. 构图具有审美质量。
5. 剧情因果容易理解。
6. 视频具有发布价值。

通用质检不能理解每条视频独有的剧情含义，因此每条视频可以按真实需要建立项目专用补充门禁。

质检是生产安全网，不是导演、画面评审或每次视觉调整的默认前置任务。只有出现以下情况时，才提前运行相关检查：

1. 不验证就无法可靠完成当前生产结果。
2. 当前修改可能破坏已经确认的关键技术条件。

普通构图、灯光、材质和审美调整不得默认触发完整质检或完整回归。

### 3.7 人工视觉审核

以下内容不能仅靠代码可靠裁定：

1. 画面是否好看。
2. 第一秒是否吸引人。
3. 资产风格是否统一。
4. 构图层次和遮挡观感。
5. 动作节奏是否自然。
6. 核心因果是否清楚。
7. 是否具有平台发布价值。

审核顺序固定为：

```text
打开并查看实际 PNG 或 MP4
→ GPT 不依赖 Claude Code 的通过结论进行独立审核
→ 判断是否满足本轮唯一目标
→ 再读取终端汇报了解技术细节
→ GPT 给出 PASS、定点修正或停止决定
→ 用户进行最终审美、继续、止损或发布裁定
```

视觉结果与文字汇报、文件统计或技术数据冲突时，实际图片和视频优先。

## 四、总生产架构

所有视频统一采用以下高层结构：

```text
用户给出目标或选题
→ GPT 冻结当前阶段和唯一任务
→ Claude Code 编写或修改确定性配置与 bpy 脚本
→ blender-mcp / Blender 执行
→ 保存版本化 .blend
→ 直接生成当前阶段的真实生产产物
→ GPT 查看实际图片或视频并初审
→ 用户审美裁定
→ 定点修正或放行
→ 必要技术门禁
→ 完整低清预览
→ 正式渲染与输出验证
→ 后期包装
→ 最终审核与交付
```

技术检查可以在不验证就无法继续时提前介入，但不得机械地在每个视觉调整之前运行完整门禁。

任何阶段都不得跳过真正必要的前置门槛，也不得把没有独立决策价值的中间步骤强行拆成任务。

## 五、通用生产原则

### 5.1 一轮一个主要目标

每轮任务开始前必须冻结：

```text
UNIQUE_MAIN_GOAL
AUTHORITATIVE_INPUTS
ALLOW_MODIFY
DO_NOT_MODIFY
ACCEPTANCE_CRITERIA
STOP_CONDITIONS
DELIVERABLES
```

每轮只允许一个主要结果，最多附带一个与其不可分割的子目标。

多个问题同时存在时，按阻断程度排序。不得在一轮中同时修改多个互不依赖的基础变量。

### 5.2 问题分类

所有新发现问题分为：

```text
BLOCKER：不解决就无法可靠完成当前目标
QUALITY：影响质量，但不阻止当前阶段继续
TECH_DEBT：架构、复用性、完备性或长期维护问题
```

只有 `BLOCKER` 自动进入当前任务。`QUALITY` 和 `TECH_DEBT` 只记录，除非用户另行授权。

审核期间不得把新发现的 `QUALITY` 或 `TECH_DEBT` 临时升级为本轮通过条件。

### 5.3 生产优先

优先级固定为：

```text
真实视频生产
→ 必要技术验证
→ 必要修复
→ 增强测试
→ 报告、证据、打包和通用化
```

不得为了让测试系统、报告或架构更完整而无限推迟视频生产。

### 5.4 生产结果驱动的任务设计

1. 每个任务必须先定义用户可以直接审核、能够推进最终视频的主要产出。
2. 主要产出通常是正式首帧、核心功能样片、完整低清预览或最终视频。
3. 导入、脚本、测试、接触表、报告和证明只能作为完成主要产出的手段，不得自动成为任务目标。
4. 简单视觉任务只保留唯一目标、权威输入、允许和禁止修改、少量决定结果是否可用的要求、交付物和停止条件。
5. 不得为了形式完整机械增加复杂 JSON、大量 `TRUE/FALSE` 字段、完整日志、证据包、多面板接触表或与本轮产出无关的测试。
6. 首帧任务直接交付实际首帧 PNG。
7. 核心样片任务直接交付实际 MP4。
8. 完整预览任务直接交付完整低清 MP4。
9. 接触表只用于资产比较、结构诊断或多个固定状态的并列查看，不得代替正式首帧、核心样片或完整预览。
10. 无法满足主要结果时必须停止并报告真实阻断，不得用替代证据、静态结果或文字声明冒充成功。

### 5.5 确定性优先

关键生产参数应进入 JSON、YAML 或明确的 Python 常量，不能只存在于 Claude Code 的临时上下文中。

同一配置、同一资产版本和同一 Blender 环境重复运行时，应生成结构一致的场景。

确定性要求服务于可重复生产，不得成为大范围重构或推迟画面产出的理由。

### 5.6 失败即停止

前置门槛失败时：

1. 不得自动进入下一阶段。
2. 不得改用其他脚本绕过。
3. 不得把部分成功包装为整体成功。
4. 不得交付替代产物作为正式结果。
5. 应保留当前最好产物并报告唯一主要阻断。

### 5.7 返工上限

同一问题默认最多：

```text
一轮实现
＋
一轮定点修正
```

第二轮后仍未解决，必须停止并由 GPT 判断：更换资产、简化目标、延期、记录技术债或让用户决定是否继续。

首帧、核心样片和完整预览不得连续产生开放式 `v3 / v4 / v5` 补丁链。

### 5.8 版本不覆盖

已通过或已提交审核的 `.blend`、配置、预览和视频不得被覆盖。

新版本使用清晰递增命名，并记录本轮唯一主要修改变量。

## 六、每条视频的标准目录

```text
projects/<VIDEO_ID>/
├── VIDEO_PLAN.md
├── design/
│   ├── VIDEO_BRIEF.md
│   ├── SCENE_REACTION_TABLE.md
│   └── ASSET_CAPABILITY_REQUIREMENTS.md
├── assets/
│   ├── ASSET_SELECTION.md
│   └── ASSET_REFERENCES.json
├── config/
│   ├── scene_config.json
│   ├── animation_config.json
│   └── render_config.json
├── scripts/
│   ├── build_scene.py
│   ├── project_gate.py
│   ├── render_preview.py
│   ├── render_final.py
│   └── validate_output.py
├── scene/
├── diagnostics/
├── reviews/
└── output/
    ├── work/
    └── delivery/
```

规则：

1. `VIDEO_PLAN.md` 是该视频当前状态的唯一权威文件。
2. `design/` 只保存已冻结的内容设计和能力要求。
3. `assets/` 保存选择、来源、许可证和本地引用，不默认复制大型第三方原始资产。
4. `config/` 保存可重复构建的参数。
5. `scripts/` 保存正式入口和生产脚本。
6. `scene/` 保存版本化 `.blend`。
7. `diagnostics/` 保存运行期技术产物，不用于最终交付。
8. `reviews/` 保存需要 GPT 或用户审核的真实图片、视频和必要结论。
9. `output/work/` 保存帧序列和临时编码。
10. `output/delivery/` 只保存本轮需要用户上传或最终交付的文件。

旧项目不要求立即重构目录。继续使用旧目录时，必须在 `VIDEO_PLAN.md` 中记录实际路径映射。

## 七、VIDEO_PLAN.md 状态字段

每条视频必须维护以下顶部字段：

```text
VIDEO_ID:
VIDEO_TITLE:
PLATFORM:
ASPECT_RATIO:
TARGET_RESOLUTION:
TARGET_FPS:
TARGET_DURATION:
CURRENT_STAGE:
ACTIVE_TASK_ID:
ACTIVE_TASK_STATUS:
UNIQUE_NEXT_TASK:
LAST_TECHNICAL_GATE:
LAST_VISUAL_REVIEW:
USER_VISUAL_APPROVAL:
CURRENT_APPROVED_BLEND:
CURRENT_APPROVED_PREVIEW:
STOP_LOSS_TRIGGERED:
FORMAL_BUILD_ENTRY:
FORMAL_GATE_ENTRY:
FORMAL_PREVIEW_ENTRY:
FORMAL_FINAL_RENDER_ENTRY:
FORMAL_OUTPUT_VALIDATION_ENTRY:
```

正文至少包含：

1. 视频目标和观众应理解的核心结论。
2. 已锁定的故事和规格。
3. 已批准资产和许可证状态。
4. 当前阶段已完成内容。
5. 已确认的技术事实。
6. 当前阻断问题。
7. 当前唯一下一任务。
8. 用户审核记录。
9. 被推迟的 `QUALITY` 和 `TECH_DEBT`。
10. 当前正式入口脚本。
11. 最终交付规格。

状态只能在真实完成并获得相应批准后更新。

某条视频在生产中发现的流程问题，只能作为附加记录存在。成片始终优先；只有阻断当前视频的通用问题才立即修复。未经后续生产验证，不得把一次性问题直接写入长期规范。

## 八、完整生产阶段

# P0：环境与入口确认

### 目标

确认当前机器、仓库和会话具备执行本视频的基本条件。

### 必须确认

1. 根目录 `CLAUDE.md` 已读取。
2. 本规范已读取。
3. 当前视频 `VIDEO_PLAN.md` 已读取。
4. Blender 可执行路径真实存在。
5. Blender 版本符合项目约束。
6. Python、FFmpeg 和必要依赖可用。
7. blender-mcp 已连接或能够启动后台 Blender。
8. 当前正式项目路径存在。
9. 当前 `ACTIVE_TASK_ID` 与用户指令一致。
10. 正式入口映射不是猜测值。

### 最小验证

新环境、MCP 修复、MCP 会话重建或关键配置变更后，按需运行：

```text
查询 Blender 状态
→ 执行最小脚本并确认真实成功标记
→ 导入或创建 1 个最小对象
→ 渲染 1 帧
→ 检查不是黑屏、空帧或纯色背景
```

已经在同一环境中通过且环境未变化时，不得每个普通生产任务都重复完整 P0 验证。

### 停止条件

环境或正式入口不满足时停止，不进入设计、资产或构建阶段。

# P1：视频定义与剧情冻结

### 输入

用户选题、目标平台和基本诉求。

### 建议输出

1. `VIDEO_BRIEF.md`
2. `SCENE_REACTION_TABLE.md`
3. 初始化后的 `VIDEO_PLAN.md`

简单视频不要求为了形式完整创建空洞文档。关键信息能够在 `VIDEO_PLAN.md` 中清楚冻结时，可以合并记录。

### 必须冻结

1. 视频题目。
2. 目标观众。
3. 观众最终应理解什么。
4. 平台、画幅、分辨率、帧率和暂定时长。
5. 第一秒钩子。
6. 核心因果。
7. 结尾结论。
8. 不在当前视频范围内的内容。

### 通过条件

每个核心剧情因果都能对应到可见的人物、设备、道具或环境变化，不能只依赖字幕解释核心事件。

### 人工审核

用户只确认选题和故事方向，不处理技术实现。

# P2：资产能力要求冻结

### 目标

在搜索资产前，定义资产必须支持的剧情功能。

### 资产分级

```text
CORE_INTERACTIVE：缺失会破坏剧情，属于 BLOCKER
SECONDARY_INTERACTIVE：最好支持，但允许简化
BACKGROUND：只负责环境可信度，不要求可动
```

### 每个核心资产至少记录

1. 所需动作和状态。
2. 必须独立控制的部件。
3. 骨骼、绑定或父子层级要求。
4. 原点和轴心要求。
5. 脚本控制要求。
6. 可接受的替代实现。
7. 功能缺失时是换资产还是简化剧情。

### 通过条件

所有核心剧情要求都能映射到明确资产能力。

# P3：资产搜索、许可证与候选裁决

### 目标

找到风格合适、许可明确、功能可实现的资产。

### 每个候选资产至少记录

```text
ASSET_NAME
ASSET_TYPE
SOURCE
LICENSE
COMMERCIAL_USE_ALLOWED
LOCAL_PATH
FORMAT
VISUAL_STYLE_MATCH
REQUIRED_ACTIONS
EXISTING_ANIMATIONS
MOVABLE_PARTS
RIG_AVAILABLE
TEXTURES_AVAILABLE
SCRIPT_CONTROLLABLE
EXPECTED_MODIFICATION
CAPABILITY_CLASS
BLOCKERS
DECISION
```

### 能力分类

```text
READY
EASY_TO_RIG
REQUIRES_MODIFICATION
UNSUITABLE
```

### 裁决原则

1. 核心资产为 `UNSUITABLE`：更换资产或简化剧情，不进入正式场景。
2. 修复成本明显高于替换成本：优先替换。
3. 许可证未知：不得进入正式商业生产或对外交付。
4. 风格明显不一致：不得因为技术可用就直接批准。
5. 候选比较应以真实资产图、演示视频和许可证来源为依据。
6. 不得让 Claude Code 自行宣布资产视觉路线正式通过。

### 输出

资产选择记录，以及供 GPT 和用户直接审核的代表性图片或视频。

# P4：新资产孤立验证与固定资产库

### 目标

在资产存在真实技术不确定性时，证明其进入正式场景所需的结构、比例、动作和材质能力可用。

### 验证原则

只验证会阻断当前视频的能力，不为每个资产机械生成完整报告和多面板接触表。

### 人物、动物和带骨骼资产的建议顺序

```text
单资产导入
→ 确认层级、骨骼和变换
→ 检查站立、朝向、比例和地面接触
→ 验证当前视频真正需要的 Action
→ 生成能够直接看清动作的短视频或代表帧
→ GPT 初审
→ 用户在需要时进行视觉裁定
→ 保存为可复用资产
```

### 人物最低技术门槛

1. 顶层控制对象明确。
2. Armature 存在。
3. Mesh 与 Armature 不分离。
4. 没有明显漂浮零件。
5. 世界空间处于合理站立状态。
6. 脚底接近地面。
7. 项目标准高度和正面方向已记录。
8. 必要 Action 真实存在并作用于正确对象。
9. 保存并重新打开后关键状态不变。

### 设备资产最低技术门槛

1. 活动部件可以独立控制或容易拆分。
2. 原点和轴心合理。
3. 所需状态之间视觉差异清楚。
4. 打开或移动后不会暴露明显空洞。
5. 通过脚本设置关键帧后行为稳定。
6. 保存后重新打开状态不变。

### 固定资产库规则

1. 正式场景优先 Append 已验证 Collection 或原生 `.blend` 资产。
2. 禁止每条视频重复导入和重新标准化同一原始资产。
3. 世界位置、旋转和缩放写在已验证的顶层控制对象。
4. Action 赋给正确的 Armature 或控制对象。
5. 不修改资产库原文件，除非启动独立资产库任务。

### 停止条件

必要核心资产存在未解决技术 `BLOCKER` 时，禁止进入正式首帧。

资产能力已经被真实结果证明后，不得为了修复诊断图、接触表或报告外观而无限停留在 P4。

# P5：静态首帧锁定

### 目标

制作一张接近最终成片效果的目标画幅正式首帧，让 GPT 和用户判断视觉路线是否值得继续。

### 核心交付

1. 版本化 `.blend`。
2. 目标画幅正式首帧 PNG。

技术摘要只在存在真实阻断或用户需要时附带。

### 分解方式

角色实例化、空间布局、摄像机构图、灯光和材质是首帧的内部工作内容，不要求机械拆成四个独立任务。

当其中某项存在独立技术 `BLOCKER` 时，可以单独验证；当资产能力已经确认且没有独立阻断时，应合并为一个正式首帧任务，直接交付真实首帧。

### 最低技术条件

1. 核心对象全部存在。
2. 人物没有明显穿模、横躺、漂浮或异常缩放。
3. 人物与设备关系符合剧情。
4. 人物脚底合理接地。
5. 朝向正确。
6. 队伍、通道或动作空间可以区分。
7. 必要对象没有被意外裁切。
8. 核心主体位于平台安全区。
9. 材质和灯光能够正常显示主要信息。

### GPT 首帧审核

1. 是否一眼能认出目标场景。
2. 人物、设备和关系是否清楚。
3. 核心因果所需的空间结构是否可见。
4. 是否存在明显遮挡、穿模、漂浮或异常放大。
5. 主体是否足够大。
6. 资产风格是否可以接受。
7. 是否接近成片，而不是诊断图或默认 Blender 练习。

### 用户裁定

```text
APPROVED
REVISION_REQUIRED
STOP
```

首帧没有 `APPROVED`，禁止进入完整动画。

# P6：3–5 秒核心功能样片

### 目标

只验证整条视频最关键、最难的互动链是否真实可做，并直接向用户展示实际效果。

### 核心交付

一段实际可播放的 3–5 秒低分辨率 MP4。

不得用静态姿势平移、接触表、骨骼采样数据或文字说明代替真实动作样片。

### 范围原则

1. 只包含核心互动。
2. 使用低分辨率预览。
3. 不做完整时长。
4. 不做最终字幕、音效和包装。
5. 不把非核心美术问题设为阻断条件。

### 必须验证

1. 资产动作确实可用。
2. Root、Armature 和 Mesh 不分离。
3. 设备状态切换清楚。
4. 关键动作顺序正确。
5. 关键道具跟随关系正确。
6. 相机能看见完整互动。
7. 功能样片可以重复构建。
8. 视频从开始到结束都能看见需要审核的主体。

### 人工门槛

GPT 和用户判断关键动作是否清楚、节奏是否可接受、是否值得扩展成完整视频。

功能样片未通过，禁止进入完整时间轴。

# P7：完整动画配置冻结

### 输出

`animation_config.json` 或等价确定性配置。

### 至少包含

1. 总帧数和帧率。
2. 各段起止帧。
3. 各角色初始状态。
4. 移动路径和关键位置。
5. 旋转和朝向变化。
6. Action 名称和切换帧。
7. 设备状态切换。
8. 道具运动和父子关系。
9. 可见性变化。
10. 摄像机关键帧。
11. 灯光变化。
12. 渲染参数。
13. 必须质检的事件帧。
14. 每个剧情段的项目专用通过条件。

### 通过条件

所有核心剧情动作都能映射到具体对象、属性、起止帧和预期状态。

不得为了配置形式完整加入当前视频不会使用的字段。

# P8：MCP 执行确定性构建

### 标准执行方式

```text
读取配置
→ 立即验证必要路径
→ 从空场景或已批准模板开始
→ Append 固定资产
→ 设置语义化对象名称
→ 写入布局、动作、设备状态、镜头和灯光
→ 保存版本化 .blend
→ 输出最小必要构建摘要
```

### 脚本要求

1. 路径常量初始化后立即检查必要存在性。
2. 优先使用语义名称，不依赖导入顺序猜对象。
3. 不使用不稳定的节点输入硬编码索引。
4. Blender 5.x 不确定 API 先做最小 smoke test。
5. 导入后重新确认 world、camera、render engine 和必要场景状态。
6. 不直接覆盖已批准 `.blend`。
7. 构建脚本和配置分离。
8. 构建阶段默认不做正式完整渲染。

### MCP 使用边界

1. 允许状态查询、场景查询、脚本执行、保存和授权渲染。
2. 不允许通过自然语言零散调用临场决定数百个参数。
3. MCP 执行失败时读取真实错误，不重复盲跑相同命令。
4. GUI 观察不能替代结构化验证。

# P9：项目技术门禁

### 目标

在完整低清预览或正式渲染前，拦截客观、可重复出现的技术错误。

每条视频应拥有项目专用正式门禁入口，或在 `VIDEO_PLAN.md` 中明确当前可用的等价入口。

不得直接运行构建脚本后宣布技术完成。

### 正式门禁流程

```text
创建唯一 run_id 和隔离运行目录
→ 构建或载入本轮 .blend
→ 发现关键事件帧
→ 运行必要的通用质检
→ 运行项目专用补充检查
→ 交叉验证结果一致性
→ 写出门禁结果
→ 决定是否授权完整预览或正式渲染
```

### 通用质检

按当前项目真实需要启用层级、站立、朝向、可见性、旋转、动画、材质、集合、地面、摄像机和投影组检查。

不得因为检查器存在，就机械启用所有字段。

### 项目专用补充检查

覆盖通用检查无法理解、但可以客观表达的剧情规则，例如：

1. 多名角色是否按指定顺序反应。
2. 某设备关闭后是否保持停止。
3. 其他设备是否继续工作。
4. 道具是否跟随正确角色。
5. 最终队伍、位置或状态是否形成预期结果。

### 门禁边界

门禁可以证明客观条件，但不能证明动作自然、构图好看或剧情具有发布价值。

### 强制规则

1. 任一必要子检查失败，技术门禁失败。
2. 门禁失败时不得授权正式渲染。
3. run_id 不匹配、结果缺失或结果不一致时必须阻止。
4. 不得用旧门禁结果授权新版本场景。
5. 普通首帧构图和材质调整不默认运行完整门禁。
6. 完整预览前至少运行与当前版本直接相关的必要门禁。

# P10：完整低清预览

### 前置条件

1. 正式首帧已批准。
2. 核心功能样片已通过。
3. 完整动画已构建。
4. 与当前版本直接相关的必要技术门禁已通过。

### 核心交付

一段完整、低分辨率、可解码的 MP4。

关键事件帧只在定位问题或核对剧情状态时按需生成；Contact Sheet 或审核板只在并列比较多个固定状态确有价值时生成，不得代替完整预览。

### 技术检查

1. 视频真实存在。
2. 非黑屏、非纯色。
3. 包含预期对象。
4. 关键动作未被裁切。
5. 视频可以完整解码。
6. 帧率、分辨率和时长符合预览要求。
7. 当前预览与当前 `.blend` 和 run_id 对应。

### GPT 视觉审核

1. 不看字幕能否理解核心因果。
2. 动作是否太快、太慢、滑行或突变。
3. 角色反应顺序是否清楚。
4. 设备状态变化是否明显。
5. 镜头是否遮挡关键事件。
6. 最终状态是否形成清楚结论。
7. 是否出现新的明显视觉错误。
8. 是否已经达到当前视频冻结的可发布级标准，或只剩不阻断发布的 `QUALITY`。

### 用户裁定

```text
APPROVED_FOR_FINAL_RENDER
REVISION_REQUIRED
STOP
```

### 修正规则

每轮只修改一个主要变量，例如：

```text
只修角色退出路径
只修某个转身时机
只修设备状态可见性
只修摄像机遮挡
```

不得在一轮同时修改角色、布局、镜头、灯光和材质。

# P11：正式渲染与输出验证

### 前置条件

1. 当前低清完整预览获得用户明确批准。
2. 必要技术门禁通过。
3. 本轮任务明确授权完整渲染。

### 正式渲染方式

默认：

```text
Blender 渲染 PNG 序列
→ 验证帧序列
→ FFmpeg 编码 MP4
→ 验证 MP4
```

不默认让 Blender 直接输出最终 MP4。

### PNG 序列最低检查

1. 文件数量等于目标帧数。
2. 首帧和末帧正确。
3. 没有缺帧。
4. 没有意外重复帧。
5. 没有损坏图片。
6. 分辨率一致且符合目标。
7. 没有意外文件混入。

### MP4 最低检查

1. 视频流存在。
2. 编码符合交付要求，默认 H.264。
3. 像素格式默认 `yuv420p`。
4. 分辨率正确。
5. 帧率正确。
6. 帧数正确。
7. 时长在容差内。
8. FFmpeg 完整解码退出码为 0。
9. 音频流数量符合当前视频设计。

当前仓库没有可直接调用的独立通用输出检查入口时，每个正式视频必须由自己的 `FORMAL_OUTPUT_VALIDATION_ENTRY` 完成全部必要检查。不能因为未来通用检查器尚未完成而跳过输出验证。

# P12：后期包装、最终审核与交付

### 后期可使用

1. 剪映。
2. jianying-mcp。
3. Remotion。
4. FFmpeg。
5. 当前项目批准的其他工具。

### 后期负责

1. 标题和字幕。
2. 箭头、高亮和辅助图形。
3. 音效和音乐。
4. 转场。
5. 封面。
6. 平台安全区。

后期只能帮助解释，不能代替 Blender 中必须真实发生的人物或设备动作。

### 最终审核

技术：

1. 最终文件和验证结果对应。
2. 编码、帧率、分辨率和时长正确。
3. 没有旧版本混入。
4. 视频可以完整播放和解码。

视觉：

1. 第一秒是否抓人。
2. 核心故事是否清楚。
3. 字幕是否遮挡主体。
4. 关键动作是否明显。
5. 结尾是否形成明确结论。
6. 用户是否愿意发布。

### 最终 delivery

只包含当前任务真正需要的文件，例如：

```text
最终 MP4
封面图
必要字幕或后期项目文件
必要的简洁验证结果
```

禁止混入：

1. 完整临时帧序列。
2. 历史失败版本。
3. 临时 `.blend`。
4. 无关测试输出。
5. 大型第三方原始资产。
6. 重复报告和无价值证据包。

## 九、固定异常排查顺序

渲染、投影或动画异常时，优先按以下顺序排查：

```text
对象和角色层级
→ 姿势与 Action
→ Root 旋转和位移
→ 尺寸与缩放
→ 地面接触
→ 世界位置
→ bbox 与投影输入
→ 摄像机
→ 灯光
→ 材质与后期
```

修复必须基于真实数据和可复现结果，不得只凭一张异常渲染图猜根因。

排查顺序不是机械门禁。已经有直接证据指向某一层时，可以定点检查该层，但不得借机扩大范围。

## 十、正式入口与禁止绕过

每条视频必须在 `VIDEO_PLAN.md` 中记录：

```text
FORMAL_BUILD_ENTRY
FORMAL_GATE_ENTRY
FORMAL_PREVIEW_ENTRY
FORMAL_FINAL_RENDER_ENTRY
FORMAL_OUTPUT_VALIDATION_ENTRY
```

旧项目通过这些字段映射真实入口，不强制迁移到统一模板目录。

字段指向的文件不存在、仍属实验脚本或尚未批准时，必须填写 `NOT_ASSIGNED` 或 `LEGACY_REFERENCE_ONLY`，不得猜测模板路径。

禁止：

1. 直接运行构建脚本后宣布整体完成。
2. 门禁失败后改用其他渲染脚本绕过。
3. 手动点击 Blender 渲染来绕过授权。
4. 使用旧 run_id 或旧结果文件授权新版本。
5. Claude Code 临时编写不受门禁控制的正式完整渲染脚本。
6. 只凭终端出现 `Render Saved` 就认定画面正确。
7. 用静态图片、数据采样或文字说明替代任务明确要求的视频产物。

允许用于诊断的独立最小脚本必须明确标记为诊断脚本，不得产生正式交付或被包装成正式结果。

## 十一、人工审核点

以下节点默认需要用户视觉裁定：

1. 最终资产路线的视觉风格。
2. 正式首帧。
3. 3–5 秒核心功能样片。
4. 完整低清预览。
5. 最终成片。

其他技术选择由 GPT 决定，Claude Code 执行。

人工审核后，由 GPT 将反馈转换为一个明确的主要修改变量和可验证任务。不得把“看着不对”“继续优化”“做到满意”为原文直接发送给 Claude Code。

在正式审核前，GPT 必须先查看真实图片或视频，不得只根据 Claude Code 的终端声明向用户推荐批准。

## 十二、每轮 Claude Code 指令结构

任务提示词的长度和结构必须服从实际复杂度，不得为了形式完整机械套用大型模板。

### 简单生产任务

正式首帧、单一画面修正、单一动画片段或单文件修改等简单任务，通常只需要：

```text
TASK_ID
UNIQUE_MAIN_GOAL
AUTHORITATIVE_INPUTS
ALLOW_MODIFY
DO_NOT_MODIFY
ACCEPTANCE_CRITERIA
STOP_CONDITIONS
DELIVERABLES
```

没有实际执行价值的背景、报告字段、验证章节和上传字段不得强行加入。

### 复杂技术任务

只有任务确实同时涉及多类输入、多个授权边界或不可分割的技术验证时，才按需增加：

```text
CURRENT_STAGE
BACKGROUND
MUST_IMPLEMENT
MUST_VERIFY
AUTHORIZED_OPERATIONS
FORBIDDEN_OPERATIONS
MUST_REPORT
MUST_UPLOAD
```

### 所有指令必须做到

1. 只有一个主要目标，最多附带一个不可分割的子目标。
2. 明确权威输入和真实路径。
3. 明确允许和禁止修改范围。
4. 明确是否授权打开 `.blend`、渲染、联网下载或完整回归。
5. 验收标准只保留决定本轮主要结果是否可用的条件。
6. 提供与真实失败原因对应的停止路径，不迫使 Claude Code 必须成功。
7. 明确最终交付物；只有用户确实需要上传文件时才要求 delivery 目录。
8. 明确不得自动进入下一阶段。
9. 汇报字段只用于陈述真实结果，不得成为实现必须满足的目标。
10. 不把 Claude Code 写成视觉批准者。

### 禁止开放式指令

```text
调整直到满意
看效果继续优化
根据画面自行批准
自动进入下一阶段
尝试不同参数找到最佳效果
顺便修复其他问题
为了通过而替换验证对象或放宽标准
```

## 十三、Claude Code 完成报告

完成报告只用于陈述本轮真实执行事实，不承担视觉批准、阶段放行或用户验收功能。

报告字段按任务需要选择，不得机械要求所有字段存在或为 `TRUE`。

### 简单视觉或单一产出任务

通常只需要：

```text
TASK_ID:
TASK_STATUS:
OUTPUT_FILES:
BLOCKERS:
VISUAL_REVIEW: PENDING_GPT_REVIEW
NEXT_STAGE_STARTED: FALSE
```

### 复杂技术任务

按需增加：

```text
CURRENT_STAGE:
UNIQUE_MAIN_GOAL:
FILES_MODIFIED:
BLENDER_EXECUTED:
REAL_BLEND_OPENED:
RENDER_EXECUTED:
TECHNICAL_GATE_RESULT:
TESTS_OR_VALIDATIONS_RUN:
FAILED_CHECKS:
PRODUCTION_PROGRESS_THIS_ROUND:
DELIVERY_FOLDER:
DELIVERY_FILES:
DELIVERY_FILE_COUNT:
UNRELATED_FILES_IN_DELIVERY:
```

### 报告规则

1. `TASK_STATUS: COMPLETED` 只表示任务明确要求的工作和交付已完成，不表示视觉审核通过。
2. 视觉任务完成后必须保持 `VISUAL_REVIEW: PENDING_GPT_REVIEW`，直到 GPT 独立审核。
3. 未授权的操作写 `NOT_AUTHORIZED`。
4. 未执行的操作写 `NOT_RUN`。
5. 失败写真实失败结果。
6. 不得为了让字段变成 `TRUE` 而修改实现、改变统计口径、跳过验证或使用替代产物。
7. 任务不需要用户上传文件时，不创建空 delivery 目录，也不输出无意义交付字段。

## 十四、交付规则

1. 只交付本轮用户真正需要审核或保存的文件。
2. 1–3 个文件默认直接上传。
3. 4 个及以上文件只有在保留目录结构、依赖关系或形成自包含交付确有价值时才生成 ZIP。
4. 普通任务不默认生成 Manifest、SHA256、完整报告、完整日志或证据包。
5. 临时脚本、缓存、帧序列、临时 `.blend` 和诊断文件不得混入正式 delivery。
6. 已批准或已提交审核的文件不得覆盖。
7. 最终视频生产优先于报告和打包。
8. 首帧、样片和完整预览必须直接提供真实 PNG 或 MP4，不得只交付 JSON、接触表或终端说明。

## 十五、止损规则

### 技术止损

1. 同一技术问题一轮实现加一轮定点修正后仍未解决：停止自动修正。
2. 连续两轮没有真实生产进展：触发止损审查。
3. 连续两轮渲染验证失败：进入独立最小诊断，不重写完整构建脚本。
4. 第二轮后仍失败：如实报告 `PARTIAL` 或真实阻断，决定换资产、简化目标或暂停。

### 资产止损

1. 核心动作需要大规模拆模、重绑或重做材质：比较替换资产成本。
2. 资产能力明显不足：换资产或改剧情，不无限修资产。
3. 风格不匹配且两轮主要视觉调整无明显改善：停止该资产路线。
4. 资产技术能力已被证明后，不得为了美化诊断接触表继续返工。

### 审美止损

正式首帧、核心样片或完整低清预览经过允许的定点返工仍未达到用户接受标准，由 GPT 提出继续、换方案或停止建议，最终由用户决定。

### 流程止损

发现 GPT 提示词、Claude Code 执行或项目规则问题时：

1. 先判断是否阻断当前生产结果。
2. 只有当前 `BLOCKER` 才立即修复。
3. 一次性画面问题只修当前画面。
4. 通用规则只有经过实际生产验证后才写入长期规范。
5. 不得为寻找流程问题而暂停成片生产。

## 十六、新对话接管协议

新的 GPT 主控对话接管视频项目时，必须按顺序真实读取：

```text
1. CLAUDE.md
2. VIDEO_PRODUCTION_EXECUTION_STANDARD.md
3. 当前视频 VIDEO_PLAN.md
4. VIDEO_PLAN 指定的当前阶段权威文件
5. 当前正式入口脚本和最近需要审核的真实交付文件
```

首次状态判断必须说明：

1. 这条视频要表达什么。
2. 当前做到哪个阶段。
3. 哪些内容已经技术通过。
4. 哪些内容已经获得用户审美批准。
5. 当前唯一阻断是什么。
6. 唯一下一任务是什么。

不得用历史对话记忆、Claude Code 终端声明或旧报告替代实际文件读取。

用户明确说“下一步”后，GPT 才生成一个当前阶段的 Claude Code 执行指令。不得一次生成多个阶段或自动推进。

当前任务依赖图片或视频时，必须读取或查看真实文件，不得仅引用文件名和终端声明。

## 十七、VIDEO_PLAN.md 模板

````markdown
# <VIDEO_TITLE> — VIDEO_PLAN

```text
VIDEO_ID:
VIDEO_TITLE:
PLATFORM:
ASPECT_RATIO:
TARGET_RESOLUTION:
TARGET_FPS:
TARGET_DURATION:
CURRENT_STAGE:
ACTIVE_TASK_ID:
ACTIVE_TASK_STATUS:
UNIQUE_NEXT_TASK:
LAST_TECHNICAL_GATE:
LAST_VISUAL_REVIEW:
USER_VISUAL_APPROVAL:
CURRENT_APPROVED_BLEND:
CURRENT_APPROVED_PREVIEW:
STOP_LOSS_TRIGGERED: FALSE
FORMAL_BUILD_ENTRY:
FORMAL_GATE_ENTRY:
FORMAL_PREVIEW_ENTRY:
FORMAL_FINAL_RENDER_ENTRY:
FORMAL_OUTPUT_VALIDATION_ENTRY:
```

## 1. 视频目标

## 2. 观众最终应理解的结论

## 3. 已锁定的故事和规格

## 4. 已批准资产与许可证

## 5. 当前阶段状态

## 6. 已确认的技术事实

## 7. 已通过的技术门槛

## 8. 用户已批准的视觉版本

## 9. 当前阻断问题

## 10. 当前唯一下一任务

## 11. 推迟的 QUALITY 与 TECH_DEBT

## 12. 正式入口

## 13. 版本与审核记录
````

## 十八、当前仓库接入原则

本规范批准后，仓库应按以下顺序接入：

1. 将本文件保存为根目录 `VIDEO_PRODUCTION_EXECUTION_STANDARD.md`。
2. 根目录 `CLAUDE.md` 只引用本规范，不复制完整流程。
3. 为当前视频更新或创建真实有效的 `VIDEO_PLAN.md`。
4. 明确该项目的正式构建、门禁、预览、最终渲染和输出验证入口。
5. 识别旧项目中哪些脚本只是历史实验、哪些仍是正式入口。
6. 状态整理完成后，立即回到当前视频的直接生产任务。

不得为了接入本规范而重写已锁定的通用质检代码、重新运行无关回归或创建新的大型流程体系。

## 十九、版本修订记录

### R2 技术审核修订

R2 处理了会影响执行真实性或入口识别的问题：

1. P0 增加真实成功标记和 stdout smoke test。
2. Live GUI 改为非默认、需单独验证和明确授权的可选能力。
3. 明确动画状态、摄像机和投影检查不等于连续运动或审美验证。
4. 旧项目通过 `VIDEO_PLAN.md` 映射真实入口。
5. 建议门禁字段改为语义要求，允许旧 schema 通过映射接入。
6. 明确输出验证不能因为通用检查器未完成而跳过。
7. Claude Code 报告以真实执行结果为基础。

### R3 生产任务设计修订

R3 处理会直接影响视频生产结果、GPT 任务设计和视觉验收真实性的问题：

1. GPT 每轮先定义可直接审核、能够推进成片的主要产出。
2. 简单视觉任务不再机械套用大型提示词结构。
3. Claude Code 只报告执行事实，不拥有视觉批准或阶段放行权。
4. GPT 必须先查看实际图片或视频，再读取完成报告。
5. 接触表和技术证明不得代替正式首帧、核心样片或完整预览。
6. 质检按真实技术阻断和破坏风险介入，不默认跟随每次视觉调整。
7. 完成报告字段不得反向驱动实现或迫使结果变成 `TRUE`。
8. 首帧内部工作不再机械拆成多个没有独立决策价值的任务。

R3 未修改通用质检代码、当前视频状态、项目脚本或 `CLAUDE.md`。

## 二十、最终原则

以后所有视频遵守下面这句话：

> 先定义能够直接推进成片的生产结果，再让 Claude Code 确定性执行；让质检代码拦截客观错误，让 GPT 先看真实画面再看报告，让用户决定审美与发布。技术验证服务于生产，不能代替生产，最终目标始终是完成并交付视频。
