# Blender 视频工厂完整生产执行规范

```text
DOCUMENT_ID: VIDEO_PRODUCTION_EXECUTION_STANDARD
VERSION: R4
DATE: 2026-07-29
PROJECT_ROOT: D:\blender-video-factory
DOCUMENT_ROLE: Blender 视频生产流程最高级通用规范
APPLIES_TO: 本仓库内所有新建和继续推进的视频项目
STATUS: PENDING_USER_APPROVAL
```

## 一、文档目的

本规范回答一件事：

> 从一个视频选题开始，如何稳定地使用 GPT、Claude Code、blender-mcp、Blender、项目质检代码和人工视觉审核，最终生成可交付的视频。

本项目的最终目标是持续产出视频，不是持续扩建测试、证据、报告、架构或通用化系统。质检、脚本、MCP 和文档都是生产工具，不得反过来成为项目目标。

本规范只记录长期稳定的通用生产规则，不记录某一条视频的临时状态、具体资产路径、当前版本或下一轮参数。每条视频的实时状态必须记录在该项目自己的 `VIDEO_PLAN.md` 中。

## 二、权威边界与优先级

不同文件只在各自范围内具有权威性，不得互相替代。

1. 用户本轮明确指令：决定本轮授权、审美裁定、是否继续、止损与发布。
2. `VIDEO_PRODUCTION_EXECUTION_STANDARD.md`：决定所有视频共同遵守的生产顺序、阶段门槛、角色边界和任务设计原则。
3. 当前视频的 `VIDEO_PLAN.md`：决定该视频的选题、已锁定内容、当前阶段、当前问题、批准状态、正式入口和唯一下一任务。
4. 根目录 `CLAUDE.md`：决定 Claude Code 的长期执行纪律、真实性、验证、交付、返工和停止规则。
5. 当前项目明确标记为 `APPROVED` 的资产模板、合同、配置和 `.blend`：只在对应项目及对应阶段内具有权威性。
6. `reviews/PROJECT_CODEIFICATION_MASTER_MAP.md`：只记录通用质检系统的实现和锁定状态，不决定某条视频当前该做什么。
7. 设计表、候选配置、资产清单和项目脚本：只在当前视频和当前阶段内生效。

发生跨范围冲突时，不得自行拼接解释。冲突会改变目标、范围、验收标准、允许修改内容或下一步时，必须停止并报告。

## 三、系统组成与职责

### 3.1 用户

用户保留以下最终权力：

1. 选题是否值得做。
2. 核心资产的视觉比例、方向和风格是否接受。
3. 世界布局和镜头构图是否接受。
4. 画面审美是否满意。
5. 是否继续投入、止损或发布。
6. 是否批准进入需要人工放行的下一阶段。

用户不负责选择 Blender API、脚本结构、测试写法、坐标计算、文件组织或技术排查方案。

### 3.2 GPT 主控

GPT 是视频项目的导演和技术主控，负责：

1. 把用户目标转化为分阶段生产计划。
2. 每轮先定义一个用户可以直接审核、能够推进最终视频的主要产出。
3. 冻结本轮唯一目标、权威输入、允许和禁止修改范围、验收标准、停止条件和交付物。
4. 决定技术路线、任务拆分、质检范围、修复顺序和技术止损。
5. 根据任务复杂度编写可直接发送给 Claude Code 的执行指令；简单任务不得机械扩写成大型规范。
6. 审核 Claude Code 的真实输出、代码、报告和交付文件。
7. 对图片和视频进行第一道视觉审核，拦截明显技术或视觉错误。
8. 视觉任务必须先查看实际 PNG 或 MP4，再读取 Claude Code 的完成报告和技术说明。
9. 视觉结果与文字汇报、文件统计或技术数据冲突时，以实际图片或视频为准。
10. 将只有审美才能决定的问题交给用户裁定。
11. 维护当前真实问题的 `PROBLEM_ID`、实现次数和定点修正次数。
12. 区分资产基准、世界布局、镜头构图、动画和输出问题，不把后层问题用于掩盖前层错误。
13. 防止测试、证据、报告、打包和架构工作挤占生产。

GPT 的技术判断属于待验证结论。Claude Code 必须通过源码、结构化数据、Blender 实际运行或诊断工具确认。

GPT 不得把视觉批准权交给 Claude Code，也不得因为 Claude Code 报告 `COMPLETED`、`PASSED` 或某字段为 `TRUE`，就跳过对真实产物、原始门禁结果和实际测量值的独立审核。

GPT 也不得通过更换 `TASK_ID`、文件版本、相机类型或方案名称，绕过同一问题的一轮实现加一轮定点修正上限。

### 3.3 Claude Code

Claude Code 是确定性施工与技术验证执行者，负责：

1. 读取本轮权威文件和明确任务。
2. 只修改授权范围内的候选配置、候选脚本、候选场景和明确允许修改的项目文件。
3. 通过 blender-mcp 或 Blender 后台进程执行 `bpy` 脚本。
4. 构建场景、设置动画、保存 `.blend`、渲染预览和运行必要质检。
5. 提取层级、坐标、旋转、骨骼、世界包围盒、最低点、可见性、动画和输出文件状态。
6. 提交本轮要求的原始门禁结果、退出码、实际值、预期值、容差和失败项。
7. 在技术条件失败时停止并报告真实失败原因。
8. 生成当前任务要求的最小交付物并停止。
9. 只报告真实执行事实，包括使用的输入、完成的操作、生成的输出和仍存在的阻断。

**TASK_STATUS 的基础取值与 `CLAUDE.md` 一致；以下补充视频候选任务与阶段批准的关系：**

- `COMPLETED`：要求的实际交付物已经生成，并且执行者检查后能够证明所有冻结验收标准均满足。不代表 GPT 或用户已经完成视觉批准，也不授权进入下一阶段。
- `PARTIAL`：已生成部分或全部交付物，但至少一项冻结验收标准仍未满足，任务未达到可靠完成状态。
- `BLOCKED`：无法在授权范围内满足冻结验收标准，或者一轮实现和一轮定点修正额度已经用完。

`TASK_STATUS` 表示本轮执行任务是否完成，不等于阶段是否批准。候选生成任务只有在“用户批准”未被写入本轮冻结验收标准时，才可以在 `USER_APPROVAL: PENDING` 的同时报告 `COMPLETED`；这仍不允许候选晋升或进入下一阶段。若本轮目标本身要求获得批准，则批准仍为 `PENDING` 时只能报告 `PARTIAL`。

文件存在、分辨率正确、对象数量正确、渲染成功或视觉辅助工具给出部分正面描述，都不能单独支持 `COMPLETED`。任一已冻结的视觉验收项被报告为不可读、缺失或不满足时，`TASK_STATUS` 不得为 `COMPLETED`。

Claude Code 不得：

1. 自行决定画面是否好看、是否具有发布价值或是否通过视觉审核。
2. 自行批准资产路线、首帧、功能样片、完整预览或最终成片。
3. 自行开始下一阶段。
4. 绕过质检门禁直接交付。
5. 通过反复盲调参数寻找“满意效果”。
6. 同时修改多个独立基础变量。
7. 把测试通过、渲染成功、文件存在或报告字段完整等同于用户已经验收。
8. 为了让汇报字段变成 `TRUE` 而改变实现、放宽标准、替换验证对象、使用替代产物或隐瞒失败。
9. 覆盖 `MANUAL_REFERENCE`、`APPROVED` 或已经提交审核的原件。
10. 在已有批准资产模板时重新导入原始资产并自行猜测比例、方向或贴地。
11. 通过自然语言自由猜测大量关键对象的世界坐标。
12. 把必检项的 `NOT_CHECKED`、结果缺失或旧结果当作通过。
13. 为适配相机偷偷移动已经批准的世界布局。

### 3.4 blender-mcp

`blender-mcp` 是外部 Blender 控制工具，不属于 `blender-video-factory` 仓库本体。它负责：

1. 查询 Blender 状态和环境。
2. 调用当前环境已经验证可用的 Blender 执行通道。
3. 执行内联或文件形式的 `bpy` 脚本。
4. 查询场景状态、保存场景和触发授权范围内的渲染。

复杂视频不得依靠数百次零散 MCP 操作逐个创建、移动和设置对象。默认方法是：

```text
批准模板 / 批准合同 / 明确配置
→ Claude Code 生成或调用确定性 bpy 脚本
→ blender-mcp 或 Blender 后台进程执行脚本
→ Blender 保存版本化候选 `.blend`
→ 全新 Blender 进程重新打开并运行阶段门禁
```

MCP 是“执行的手”，不是导演、动画设计师或质检员。工具名称可能随安装版本变化，执行时以当前 MCP 服务真实暴露的工具为准。

### 3.5 Blender

Blender 是实际生产环境，负责：

1. 资产导入与 Append。
2. 场景、材质、灯光、摄像机和动画计算。
3. `.blend` 保存。
4. PNG、帧序列和预览视频渲染。

默认优先使用可重复的后台执行。Live GUI 不是生产前提，也不能赋予 Claude Code可靠的视觉判断能力。只有当前环境单独验证通过并且本轮明确授权时，才用于用户观察或人工检查。

### 3.6 通用质检系统

`protocol_guard/phase3_min` 是通用场景与资产质检底座，负责客观、可重复判断的要求，例如：

1. 对象存在性和类型。
2. 层级。
3. 站立。
4. 朝向。
5. 可见性。
6. 旋转。
7. 采样帧中的 Action、可见性和已配置状态字段。
8. 材质分配。
9. Collection 规则。
10. 地面接触。
11. 已配置摄像机结构与条件检查。
12. 已配置投影组的包围盒、角点和屏幕范围检查。

这些检查只证明配置中声明且实际执行的客观条件。它们不证明：

1. 帧间运动连续。
2. 动作自然。
3. 整个时间轴始终无遮挡。
4. 构图具有审美质量。
5. 剧情因果容易理解。
6. 视频具有发布价值。

通用质检不能理解每条视频独有的剧情含义，因此每条视频可以按真实需要建立项目专用补充门禁。

检查器存在于仓库，不等于当前生产场景已经受检。项目门禁必须明确：

1. 当前阶段哪些对象和字段是必检项。
2. 实际调用哪些通用检查器。
3. 哪些尺寸、变换、队伍人数、剧情状态或输出规格需要项目专用检查。
4. 门禁结果对应的 `.blend`、run_id 和实际执行版本。
5. 是否允许进入下一阶段。

项目声明为必检的项目返回 `FAIL`、`ERROR`、`NOT_CHECKED`、结果缺失、旧结果或 run_id 不匹配时，项目门禁均必须失败。

质检是生产安全网，不是导演、画面评审或每次视觉调整的默认前置任务。只有出现以下情况时，才提前运行相关检查：

1. 不验证就无法可靠完成当前生产结果。
2. 当前修改可能破坏已经确认的关键技术条件。

普通构图、灯光、材质和审美调整不得默认触发完整质检或完整回归。

### 3.7 人工视觉审核与三层结果

视觉结果必须明确区分：

```text
MACHINE_GATE_RESULT
INDEPENDENT_VISUAL_REVIEW
USER_APPROVAL
```

机器门禁负责对象数量、世界尺寸、最低点、变换、视锥、投影范围、关键裁切、硬失败屏幕占比和文件规格等可计算事实。

GPT 独立视觉审核负责明显遮挡、对象关系不可读、主体过小、构图明显失衡和剧情表达失败。

用户负责最终审美、是否继续、阶段批准和发布。用户可以拒绝任何机器 `PASS` 或 GPT 推荐，但不能在不修改候选或不正式修订合同的情况下把机器硬失败直接改写为通过。Claude Code 不得填写或暗示 `USER_APPROVAL: APPROVED`。

以下内容不能仅靠代码可靠裁定：

1. 画面是否好看。
2. 第一秒是否吸引人。
3. 资产风格是否统一。
4. 构图层次和遮挡观感。
5. 动作节奏是否自然。
6. 核心因果是否清楚。
7. 是否具有平台发布价值。

审核顺序固定为：

```text
打开并查看实际 PNG 或 MP4
→ GPT 不依赖 Claude Code 的通过结论进行独立审核
→ 判断是否满足本轮唯一目标
→ 再读取终端汇报了解技术细节
→ GPT 给出 PASS、定点修正或停止决定
→ 用户进行最终审美、继续、止损或发布裁定
```

视觉结果与文字汇报、文件统计或技术数据冲突时，实际图片和视频优先；可计算事实与目测冲突时，应先核对原始机器结果和真实目标配置。

“场景中存在对象”和“观众能够看见并区分对象”是两种不同验收。视觉上看不清本身就是失败，不能根据场景数据推定观众已经看见。

## 四、总生产架构

所有视频统一采用以下高层结构：

```text
用户给出目标或选题
→ GPT 冻结当前阶段、`PROBLEM_ID` 和唯一任务
→ 读取批准输入或明确的人工参考
→ Claude Code 编写或调用确定性配置与 bpy 脚本
→ blender-mcp / Blender 执行
→ 保存版本化候选 `.blend`
→ 全新 Blender 进程重新打开
→ 运行当前阶段必要门禁
→ 生成当前阶段的真实生产产物
→ GPT 查看实际图片或视频并独立初审
→ 用户进行需要的视觉批准
→ 候选晋升为当前阶段批准版本，或进行一次定点修正
→ 正式首帧批准后制作 3–5 秒核心功能样片
→ 核心功能样片通过后冻结完整动画配置
→ 确定性构建完整动画候选
→ 运行 G3 动画门禁与 P9 汇总门禁
→ 完整低清预览
→ 正式渲染与输出验证
→ 后期包装
→ 最终审核与交付
```

技术检查可以在不验证就无法继续时提前介入，但不得机械地在每个视觉调整之前运行完整门禁。

任何阶段都不得跳过真正必要的前置门槛，也不得把没有独立决策价值的中间步骤强行拆成任务。

## 五、通用生产原则

### 5.1 一轮一个主要目标

每轮任务开始前必须冻结：

```text
PROBLEM_ID
TASK_ID
CURRENT_STAGE
UNIQUE_MAIN_GOAL
AUTHORITATIVE_INPUTS
INPUT_APPROVAL_STATUS
ALLOW_MODIFY
DO_NOT_MODIFY
MANDATORY_GATE
ACCEPTANCE_CRITERIA
STOP_CONDITIONS
DELIVERABLES
```

不属于返工或持续问题的简单任务可以不设置 `PROBLEM_ID`。只要任务继续处理同一真实问题，就必须沿用同一 `PROBLEM_ID`。其他不适用字段应明确写为 `NOT_REQUIRED` 或 `NOT_APPLICABLE`，不得为了形式完整启动无关门禁。

每轮只允许一个主要结果，最多附带一个与其不可分割的子目标。

多个问题同时存在时，按阻断程度排序。不得在一轮中同时修改多个互不依赖的基础变量。

### 5.2 问题分类

所有新发现问题分为：

```text
BLOCKER：不解决就无法可靠完成当前目标
QUALITY：影响质量，但不阻止当前阶段继续
TECH_DEBT：架构、复用性、完备性或长期维护问题
```

只有 `BLOCKER` 自动进入当前任务。`QUALITY` 和 `TECH_DEBT` 只记录，除非用户另行授权。

审核期间不得把新发现的 `QUALITY` 或 `TECH_DEBT` 临时升级为本轮通过条件。

**冻结验收标准的地位：**

任务开始前已经写入目标或验收标准的画面质量、可读性、功能完整性、正确性和可用性要求，属于当前目标不可分割的组成部分。任一冻结验收标准未满足时：

1. 不得将其降级为 `QUALITY` 或"已知限制"后宣布任务完成。
2. 必须先完成定点修正。修正额度用完后仍不满足，必须报告 `PARTIAL` 或 `BLOCKED`。
3. 只有执行过程中新发现、且不影响已冻结目标和验收标准的问题，才归类为 `QUALITY` 或 `TECH_DEBT`。
4. 文件存在、分辨率正确、对象数量正确、渲染成功或视觉辅助工具给出部分正面描述，都不能替代冻结验收标准的逐项确认。

### 5.3 生产优先

优先级固定为：

```text
真实视频生产
→ 必要技术验证
→ 必要修复
→ 增强测试
→ 报告、证据、打包和通用化
```

不得为了让测试系统、报告或架构更完整而无限推迟视频生产。

### 5.4 生产结果驱动的任务设计

1. 普通生产任务必须先定义用户可以直接审核、能够推进最终视频的主要产出。
2. 主要产出通常是正式首帧、核心功能样片、完整低清预览或最终视频。
3. 导入、脚本、测试、接触表、报告和证明只能作为完成主要产出的手段，不得自动成为普通生产任务的主要目标。
4. 当资产测量、批准模板派生、门禁接入、故障注入或最小诊断属于解除当前 `BLOCKER` 的必要前置工作时，可以作为独立技术任务。该任务必须直接解除当前生产阻断，只运行最小必要验证，并在完成后立即回到直接生产。
5. 简单视觉任务只保留唯一目标、权威输入、允许和禁止修改、少量决定结果是否可用的要求、交付物和停止条件。
6. 不得为了形式完整机械增加复杂 JSON、大量 `TRUE/FALSE` 字段、完整日志、证据包、多面板接触表或与本轮产出无关的测试。
7. 首帧任务直接交付实际首帧 PNG。
8. 核心样片任务直接交付实际 MP4。
9. 完整预览任务直接交付完整低清 MP4。
10. 接触表只用于资产比较、结构诊断或多个固定状态的并列查看，不得代替正式首帧、核心样片或完整预览。
11. 无法满足主要结果时必须停止并报告真实阻断，不得用替代证据、静态结果或文字声明冒充成功。

### 5.5 确定性优先

关键生产参数应进入已批准的 JSON、YAML、明确的 Python 常量、资产模板或 `.blend`，不能只存在于 Claude Code 的临时上下文中。

核心资产的世界尺寸、最低点、方向、统一控制父级和交互锚点，以及复杂布局中的对象身份、数量、分组、间距、朝向和初始变换，应在进入后续阶段前形成可读取的批准基准。

从 `MANUAL_REFERENCE` 提取布局合同时，必须区分用户真正批准的语义关系与当前场景偶然存在的坐标。队伍数量、分组、方向、锚点关系和合理间距可以进入批准合同；原始世界坐标可以作为基准快照，但未经明确批准不得自动提升为所有场景通用规则。

同一批准配置、同一批准资产版本和同一 Blender 环境重复运行时，应生成结构及关键世界数据一致的场景。

确定性要求服务于可重复生产，不得成为大范围重构或推迟画面产出的理由。

### 5.6 失败即停止

前置门槛失败时：

1. 不得自动进入下一阶段。
2. 不得改用其他脚本绕过。
3. 不得把部分成功包装为整体成功。
4. 不得交付替代产物作为正式结果。
5. 应保留当前最好产物并报告唯一主要阻断。
6. 约束可能互相冲突时，应报告需要用户决定的前提选项，不得通过继续盲调来宣称“不可实现”或强行完成。

### 5.7 问题级返工上限

同一 `PROBLEM_ID` 默认最多：

```text
一轮实现
＋
一轮定点修正
```

更换 `TASK_ID`、脚本名、文件版本、相机类型、焦距或方案名称，不重置次数。

第二轮后仍未解决，必须：

1. 设置 `STOP_LOSS_TRIGGERED: TRUE`。
2. 停止自动修正。
3. 回到资产、画幅、分镜、布局、技术前提或目标决策。
4. 由用户决定是否重新授权继续。

首帧、核心样片和完整预览不得连续产生开放式 `v3 / v4 / v5` 补丁链。

### 5.8 版本不覆盖

已批准、人工参考或已提交审核的 `.blend`、配置、预览和视频不得被覆盖。

新版本使用清晰递增命名，并记录本轮唯一主要修改变量。

### 5.9 产物身份与晋升

关键生产文件必须明确身份：

```text
MANUAL_REFERENCE：用户手动确认的参考答案，只读，不自动等于批准模板
CANDIDATE：当前阶段候选，允许在授权范围内修改
APPROVED：必要机器门禁通过，并获得该阶段规定的批准；技术文件可由 GPT 技术批准，资产比例、世界布局、镜头、样片、完整预览和成片按本规范要求由用户批准
```

强制规则：

1. Claude Code 只能从 `APPROVED` 或任务明确指定的 `MANUAL_REFERENCE` 副本派生候选。
2. 候选门禁失败或人工未批准时，不得成为下一阶段输入。
3. 下一阶段默认只能读取 `APPROVED`。
4. 批准内容需要变化时，必须创建新候选并重新审批。
5. 关键 `.blend` 必须保存后由全新 Blender 进程重新打开并验证；只检查当前内存状态不构成批准证据。

### 5.10 世界布局与镜头构图分离

必须区分：

```text
WORLD_LAYOUT_APPROVED
SHOT_COMPOSITION_APPROVED
```

`WORLD_LAYOUT_APPROVED` 证明资产尺寸、空间关系、队伍和动画路线合理。

`SHOT_COMPOSITION_APPROVED` 证明在某一画幅和具体镜头中，叙事信息与构图获得批准。

相机失败不得自动推翻世界布局；世界布局已批准也不意味着一个镜头必须完整容纳全部对象。需要时应拆镜头、调整分镜，或由用户正式修改画幅和首帧目标。相机任务不得为适配画面偷偷移动批准布局。

工程验证画幅与最终发布画幅不同时，必须在 `VIDEO_PLAN.md` 中分别记录，不能临时偷换。

## 六、每条视频的标准目录

```text
projects/<VIDEO_ID>/
├── VIDEO_PLAN.md
├── design/
│   ├── VIDEO_BRIEF.md
│   ├── SCENE_REACTION_TABLE.md
│   └── ASSET_CAPABILITY_REQUIREMENTS.md
├── assets/
│   ├── ASSET_SELECTION.md
│   ├── ASSET_REFERENCES.json
│   └── approved_templates/
├── config/
│   ├── candidates/
│   ├── approved/
│   ├── scene_config.json
│   ├── animation_config.json
│   └── render_config.json
├── scripts/
│   ├── build_scene.py
│   ├── project_gate.py
│   ├── render_preview.py
│   ├── render_final.py
│   └── validate_output.py
├── scene/
│   ├── manual_reference/
│   ├── candidates/
│   └── approved/
├── diagnostics/
├── reviews/
└── output/
    ├── work/
    └── delivery/
```

规则：

1. `VIDEO_PLAN.md` 是该视频当前状态的唯一权威文件。
2. `design/` 只保存已冻结的内容设计和能力要求。
3. `assets/` 保存选择、来源、许可证和本地引用，不默认复制大型第三方原始资产。
4. `config/` 保存可重复构建的候选与批准参数；批准合同不得由普通生产任务覆盖。
5. `scripts/` 保存正式入口和生产脚本。
6. `scene/` 保存版本化 `.blend`，并尽量区分人工参考、候选和批准版本。
7. `diagnostics/` 保存运行期技术产物，不用于最终交付。
8. `reviews/` 保存需要 GPT 或用户审核的真实图片、视频和必要结论。
9. `output/work/` 保存帧序列和临时编码。
10. `output/delivery/` 只保存本轮需要用户上传或最终交付的文件。

旧项目不要求立即重构目录。继续使用旧目录时，必须在 `VIDEO_PLAN.md` 中记录实际路径映射。

## 七、VIDEO_PLAN.md 状态字段

每条视频必须维护以下顶部字段：

```text
VIDEO_ID:
VIDEO_TITLE:
PLATFORM:
ASPECT_RATIO:
ENGINEERING_VALIDATION_ASPECT_RATIO:
TARGET_RESOLUTION:
TARGET_FPS:
TARGET_DURATION:
CURRENT_STAGE:
CURRENT_PROBLEM_ID:
PROBLEM_IMPLEMENTATION_COUNT:
PROBLEM_CORRECTION_COUNT:
ACTIVE_TASK_ID:
ACTIVE_TASK_STATUS:
UNIQUE_NEXT_TASK:
LAST_TECHNICAL_GATE:
LAST_VISUAL_REVIEW:
USER_VISUAL_APPROVAL:
CURRENT_MANUAL_REFERENCE:
CURRENT_APPROVED_ASSET_TEMPLATE:
CURRENT_APPROVED_WORLD_LAYOUT:
CURRENT_APPROVED_SHOT:
CURRENT_APPROVED_BLEND:
CURRENT_APPROVED_PREVIEW:
STOP_LOSS_TRIGGERED:
FORMAL_BUILD_ENTRY:
FORMAL_GATE_ENTRY:
FORMAL_PREVIEW_ENTRY:
FORMAL_FINAL_RENDER_ENTRY:
FORMAL_OUTPUT_VALIDATION_ENTRY:
```

字段语义：

1. `ASPECT_RATIO` 始终表示最终发布画幅。
2. `ENGINEERING_VALIDATION_ASPECT_RATIO` 只表示工程样片或链路验证画幅；没有单独工程画幅时，与 `ASPECT_RATIO` 填写相同值。
3. `CURRENT_APPROVED_BLEND` 指向当前阶段最新的批准 `.blend`；`CURRENT_APPROVED_WORLD_LAYOUT` 和 `CURRENT_APPROVED_SHOT` 进一步说明它承担的批准角色。
4. 未产生真实批准结果的字段必须填写 `NONE`、`NOT_ASSIGNED` 或等价明确状态，不得根据文件名推断。

正文至少包含：

1. 视频目标和观众应理解的核心结论。
2. 已锁定的故事和规格。
3. 已批准资产和许可证状态。
4. 当前阶段已完成内容。
5. 已确认的技术事实。
6. 当前阻断问题、`PROBLEM_ID`、实现次数、修正次数和止损状态。
7. 当前唯一下一任务。
8. 用户审核记录。
9. 人工参考、候选和批准文件的真实路径与身份。
10. 被推迟的 `QUALITY` 和 `TECH_DEBT`。
11. 当前正式入口脚本。
12. 最终交付规格。

状态只能在真实完成并获得相应批准后更新。

某条视频在生产中发现的流程问题，只能作为附加记录存在。成片始终优先；只有阻断当前视频的通用问题才立即修复。未经后续生产验证，不得把一次性问题直接写入长期规范。

## 八、完整生产阶段

# P0：环境与入口确认

### 目标

确认当前机器、仓库和会话具备执行本视频的基本条件。

### 必须确认

1. 根目录 `CLAUDE.md` 已读取。
2. 本规范已读取。
3. 当前视频 `VIDEO_PLAN.md` 已读取。
4. Blender 可执行路径真实存在。
5. Blender 版本符合项目约束。
6. Python、FFmpeg 和必要依赖可用。
7. blender-mcp 已连接或能够启动后台 Blender。
8. 当前正式项目路径存在。
9. 当前 `ACTIVE_TASK_ID` 与用户指令一致。
10. 当前任务实际依赖的正式入口映射不是猜测值；尚未进入对应阶段的入口允许为 `NOT_ASSIGNED`。

### 最小验证

新环境、MCP 修复、MCP 会话重建或关键配置变更后，按需运行：

```text
查询 Blender 状态
→ 执行最小脚本并确认真实成功标记
→ 导入或创建 1 个最小对象
→ 渲染 1 帧
→ 检查不是黑屏、空帧或纯色背景
```

已经在同一环境中通过且环境未变化时，不得每个普通生产任务都重复完整 P0 验证。

### 停止条件

环境不满足时停止。当前任务实际依赖的正式入口缺失、不真实或未验证时，只阻断依赖该入口的任务。尚未进入对应阶段的入口为 `NOT_ASSIGNED` 时，不阻断更早阶段的设计、资产或基础构建。

# P1：视频定义与剧情冻结

### 输入

用户选题、目标平台和基本诉求。

### 建议输出

1. `VIDEO_BRIEF.md`
2. `SCENE_REACTION_TABLE.md`
3. 初始化后的 `VIDEO_PLAN.md`

简单视频不要求为了形式完整创建空洞文档。关键信息能够在 `VIDEO_PLAN.md` 中清楚冻结时，可以合并记录。

### 必须冻结

1. 视频题目。
2. 目标观众。
3. 观众最终应理解什么。
4. 平台、画幅、分辨率、帧率和暂定时长。
5. 第一秒钩子。
6. 核心因果。
7. 结尾结论。
8. 不在当前视频范围内的内容。

### 通过条件

每个核心剧情因果都能对应到可见的人物、设备、道具或环境变化，不能只依赖字幕解释核心事件。

### 人工审核

用户只确认选题和故事方向，不处理技术实现。

# P2：资产能力要求冻结

### 目标

在搜索资产前，定义资产必须支持的剧情功能。

### 资产分级

```text
CORE_INTERACTIVE：缺失会破坏剧情，属于 BLOCKER
SECONDARY_INTERACTIVE：最好支持，但允许简化
BACKGROUND：只负责环境可信度，不要求可动
```

### 每个核心资产至少记录

1. 所需动作和状态。
2. 必须独立控制的部件。
3. 骨骼、绑定或父子层级要求。
4. 原点和轴心要求。
5. 脚本控制要求。
6. 可接受的替代实现。
7. 功能缺失时是换资产还是简化剧情。

### 通过条件

所有核心剧情要求都能映射到明确资产能力。

# P3：资产搜索、许可证与候选裁决

### 目标

找到风格合适、许可明确、功能可实现的资产。

### 每个候选资产至少记录

```text
ASSET_NAME
ASSET_TYPE
SOURCE
LICENSE
COMMERCIAL_USE_ALLOWED
LOCAL_PATH
FORMAT
VISUAL_STYLE_MATCH
REQUIRED_ACTIONS
EXISTING_ANIMATIONS
MOVABLE_PARTS
RIG_AVAILABLE
TEXTURES_AVAILABLE
SCRIPT_CONTROLLABLE
EXPECTED_MODIFICATION
CAPABILITY_CLASS
BLOCKERS
DECISION
```

### 能力分类

```text
READY
EASY_TO_RIG
REQUIRES_MODIFICATION
UNSUITABLE
```

### 裁决原则

1. 核心资产为 `UNSUITABLE`：更换资产或简化剧情，不进入正式场景。
2. 修复成本明显高于替换成本：优先替换。
3. 许可证未知：不得进入正式商业生产或对外交付。
4. 风格明显不一致：不得因为技术可用就直接批准。
5. 候选比较应以真实资产图、演示视频和许可证来源为依据。
6. 不得让 Claude Code 自行宣布资产视觉路线正式通过。

### 输出

资产选择记录，以及供 GPT 和用户直接审核的代表性图片或视频。

# P4：新资产孤立验证与固定资产库

### 目标

在资产存在真实技术不确定性时，证明其进入正式场景所需的结构、比例、动作和材质能力可用。

### 验证原则

只验证会阻断当前视频的能力，不为每个资产机械生成完整报告和多面板接触表。

### 人物、动物和带骨骼资产的建议顺序

```text
单资产导入
→ 确认层级、骨骼和变换
→ 检查站立、朝向、比例和地面接触
→ 验证当前视频真正需要的 Action
→ 生成能够直接看清动作的短视频或代表帧
→ GPT 初审
→ 用户在需要时进行视觉裁定
→ 保存为可复用资产
```

### 人物最低技术门槛

1. 顶层控制对象明确。
2. Armature 存在。
3. Mesh 与 Armature 不分离。
4. 没有明显漂浮零件。
5. 世界空间处于合理站立状态。
6. 脚底接近地面。
7. 项目标准高度和正面方向已记录。
8. 必要 Action 真实存在并作用于正确对象。
9. 保存并重新打开后关键状态不变。

### 设备资产最低技术门槛

1. 活动部件可以独立控制或容易拆分。
2. 可见几何的组合世界包围盒尺寸已经记录。
3. 世界空间最低点、地面高度和贴地容差已经记录。
4. 正面、顾客侧、队伍延伸方向和必要轴向已经明确。
5. 统一控制父级和子 Mesh 结构明确。
6. 所需状态之间视觉差异清楚。
7. 打开或移动后不会暴露明显空洞。
8. 必要交互锚点已经建立或有明确标定依据。按项目需要可使用 `ASSET_ROOT`、`QUEUE_START`、`QUEUE_DIRECTION`、`CUSTOMER_TARGET`、`OPERATOR_POSITION`、`EXIT_POINT`、`INTERACTION_POINT` 等语义名称。
9. 通过脚本设置关键帧后行为稳定。
10. 保存后重新打开状态不变。

模型是否正确以可见几何的世界空间结果为准，不得只根据父级 `location`、`rotation`、`scale` 或原点判断。

### 固定资产库与批准模板规则

1. 正式场景优先 Append 已批准 Collection 或原生 `.blend` 模板。
2. 禁止每条视频重复导入和重新标准化同一原始资产。
3. 世界位置、旋转和缩放写在已验证的顶层控制对象。
4. Action 赋给正确的 Armature 或控制对象。
5. 不修改批准资产库原文件；需要变化时创建候选模板版本。
6. 核心资产模板未获得必要机器门禁和用户视觉批准时，不能作为正式布局的可靠输入。

已有用户手动确认场景时，必须采用无损派生：

```text
冻结 MANUAL_REFERENCE 原件
→ 第一轮只读取和测量
→ 在副本中派生候选模板
→ 比较世界尺寸、最低点、结构和统一检查视图
→ 保存并由全新 Blender 进程重开
→ 用户确认视觉未变化
→ 晋升为 APPROVED
```

不得重新导入原始资产，不得根据面板 Scale 数字猜最终尺寸，也不得为了追求所有 Scale 为 `1,1,1` 而破坏正确的可见几何。

### 停止条件

必要核心资产存在未解决技术 `BLOCKER` 时，禁止进入正式首帧。

资产能力已经被真实结果证明后，不得为了修复诊断图、接触表或报告外观而无限停留在 P4。

# P5：静态首帧锁定

### 目标

制作一张接近最终成片效果的目标画幅正式首帧，让 GPT 和用户判断视觉路线是否值得继续。

### 核心交付

1. 版本化 `.blend`。
2. 目标画幅正式首帧 PNG。

技术摘要只在存在真实阻断或用户需要时附带。

### 分解方式

资产模板和世界布局属于首帧的前置批准，不得与相机施工混为同一基础变量。资产模板和世界布局已经批准后，首次首帧构建不要求把摄像机构图、基础灯光和基础材质机械拆成多个任务。

但存在以下任一情况时，必须先完成对应基础批准，不得直接依靠相机硬解：

1. 核心资产的世界尺寸、最低点、方向或父级尚未批准。
2. Claude Code 曾多次无法可靠决定核心资产比例或位置。
3. 世界布局没有可重复生成的合同或脚本。
4. 当前相机任务可能通过移动资产掩盖布局错误。

P5 至少区分两个批准点：

```text
P5A WORLD_LAYOUT_APPROVAL
P5B SHOT_COMPOSITION_APPROVAL
```

P5A 只能从批准资产模板和批准布局合同生成世界布局候选。布局门禁通过并获得用户确认后，形成 `WORLD_LAYOUT_APPROVED`。为了审核世界布局，可以生成正面、侧面、俯视和透视等诊断视图；这些诊断相机和诊断图片不得冒充正式镜头或正式首帧。

P5B 只能从批准世界布局创建相机候选。相机任务不得移动批准布局。实现轮默认只生成不超过 3 个预先定义的相机候选，不得进行开放式参数搜索。相机门禁通过、GPT 独立初审通过并获得用户确认后，形成 `SHOT_COMPOSITION_APPROVED`。若实现轮的限定候选全部失败，不得在同一轮继续生成第四、第五个候选；若同一 `PROBLEM_ID` 尚有一次定点修正额度，修正轮只能选择其中一个候选，依据明确失败原因修改该候选，不得创建新的第四种相机方案。修正后仍失败，应回到画幅、分镜或镜头合同决策。

当资产模板和世界布局已经批准且没有独立阻断时，首次首帧构建允许将相机、基础灯光和基础材质合并为一个直接交付真实 PNG 的任务。针对已经生成的首帧进行修正时，每轮仍只能修改一个主要变量，不得同时改动相机、灯光、材质或布局。

### 最低技术条件

1. 核心对象全部存在。
2. 人物没有明显穿模、横躺、漂浮或异常缩放。
3. 人物与设备关系符合剧情。
4. 人物脚底合理接地。
5. 朝向正确。
6. 队伍、通道或动作空间可以区分。
7. 必要对象没有被意外裁切。
8. 核心主体位于平台安全区。
9. 材质和灯光能够正常显示主要信息。

### GPT 首帧审核

1. 是否一眼能认出目标场景。
2. 人物、设备和关系是否清楚。
3. 核心因果所需的空间结构是否可见。
4. 是否存在明显遮挡、穿模、漂浮或异常放大。
5. 主体是否足够大。
6. 资产风格是否可以接受。
7. 是否接近成片，而不是诊断图或默认 Blender 练习。

### 用户裁定

```text
APPROVED
REVISION_REQUIRED
STOP
```

世界布局没有 `WORLD_LAYOUT_APPROVED`，不得进入相机定稿；镜头没有 `SHOT_COMPOSITION_APPROVED`，不得进入完整动画。

# P6：3–5 秒核心功能样片

### 目标

只验证整条视频最关键、最难的互动链是否真实可做，并直接向用户展示实际效果。

### 核心交付

一段实际可播放的 3–5 秒低分辨率 MP4。

不得用静态姿势平移、接触表、骨骼采样数据或文字说明代替真实动作样片。

### 范围原则

1. 只包含核心互动。
2. 使用低分辨率预览。
3. 不做完整时长。
4. 不做最终字幕、音效和包装。
5. 不把非核心美术问题设为阻断条件。

### 必须验证

1. 资产动作确实可用。
2. Root、Armature 和 Mesh 不分离。
3. 设备状态切换清楚。
4. 关键动作顺序正确。
5. 关键道具跟随关系正确。
6. 相机能看见完整互动。
7. 功能样片可以重复构建。
8. 视频从开始到结束都能看见需要审核的主体。

### 人工门槛

GPT 和用户判断关键动作是否清楚、节奏是否可接受、是否值得扩展成完整视频。

功能样片未通过，禁止进入完整时间轴。

当生产链刚完成资产模板、确定性布局或门禁接入等基础修正时，应优先使用真实资产完成最小端到端样片，至少贯通：

```text
批准资产模板
→ 确定性世界布局
→ 强制布局门禁
→ 批准相机
→ 一次核心设备状态变化
→ 至少一段真实人物移动
→ 可解码视频输出
```

不得用立方体等与真实资产无关的替代物证明真实生产链已经可靠。工程验证画幅可以与最终发布画幅不同，但必须在 `VIDEO_PLAN.md` 中明确记录。

# P7：完整动画配置冻结

### 输出

`animation_config.json` 或等价确定性配置。

### 至少包含

1. 总帧数和帧率。
2. 各段起止帧。
3. 各角色初始状态。
4. 移动路径和关键位置。
5. 旋转和朝向变化。
6. Action 名称和切换帧。
7. 设备状态切换。
8. 道具运动和父子关系。
9. 可见性变化。
10. 摄像机关键帧。
11. 灯光变化。
12. 渲染参数。
13. 必须质检的事件帧。
14. 每个剧情段的项目专用通过条件。

### 通过条件

所有核心剧情动作都能映射到具体对象、属性、起止帧和预期状态。

不得为了配置形式完整加入当前视频不会使用的字段。

# P8：MCP 执行确定性构建

### 标准执行方式

```text
读取批准配置
→ 立即验证必要路径
→ 从批准模板或批准场景开始
→ Append 批准资产
→ 设置语义化对象名称
→ 只写入当前阶段允许的布局、动作、设备状态、镜头或灯光
→ 保存版本化候选 `.blend`
→ 关闭施工进程
→ 全新 Blender 进程重新打开候选
→ 输出最小必要构建摘要并运行本阶段门禁
```

### 脚本要求

1. 路径常量初始化后立即检查必要存在性。
2. 优先使用语义名称，不依赖导入顺序猜对象。
3. 不使用不稳定的节点输入硬编码索引。
4. Blender 5.x 不确定 API 先做最小 smoke test。
5. 导入后重新确认 world、camera、render engine 和必要场景状态。
6. 不直接覆盖 `MANUAL_REFERENCE`、已批准 `.blend` 或批准合同。
7. 构建脚本和配置分离。
8. 构建阶段默认不做正式完整渲染。
9. 只输出候选文件；门禁失败或人工未批准时不得进入下一阶段。

### MCP 使用边界

1. 允许状态查询、场景查询、脚本执行、保存和授权渲染。
2. 不允许通过自然语言零散调用临场决定数百个参数。
3. MCP 执行失败时读取真实错误，不重复盲跑相同命令。
4. GUI 观察不能替代结构化验证。

# P9：项目技术门禁与统一编排

### 目标

在阶段转换、完整低清预览或正式渲染前，拦截客观、可重复出现的技术错误。

P9 定义通用门禁机制和统一入口，不表示所有检查都推迟到 P8 之后。`G0`–`G3` 必须在对应资产、布局、相机和动画阶段被调用；P9 还负责完整预览前的汇总门禁。

每条视频应拥有项目专用正式门禁入口，或在 `VIDEO_PLAN.md` 中明确当前可用的等价入口。

不得直接运行构建脚本后宣布技术完成。

### 正式门禁流程

```text
创建唯一 run_id 和隔离运行目录
→ 保存或载入本轮候选 `.blend`
→ 全新 Blender 进程重新打开
→ 发现关键事件帧
→ 运行必要的通用质检
→ 运行项目专用补充检查
→ 交叉验证结果、版本、blend_path 和 run_id
→ 写出原始门禁结果
→ 返回明确退出码
→ 决定是否允许进入下一阶段
```

### 通用质检

按当前项目真实需要启用层级、站立、朝向、可见性、旋转、动画、材质、集合、地面、摄像机和投影组检查。

不得因为检查器存在，就机械启用所有字段。

### 项目专用补充检查

覆盖通用检查无法理解、但可以客观表达的剧情规则，例如：

1. 多名角色是否按指定顺序反应。
2. 某设备关闭后是否保持停止。
3. 其他设备是否继续工作。
4. 道具是否跟随正确角色。
5. 最终队伍、位置或状态是否形成预期结果。

### 阶段门禁模型

每条视频按真实需要编排以下最小门禁，不强制所有项目使用相同脚本名：

```text
G0 ASSET_GATE
G1 LAYOUT_GATE
G2 CAMERA_GATE
G3 ANIMATION_GATE
G4 OUTPUT_GATE
```

- `G0`：结构、世界尺寸、最低点、方向、锚点、材质、可控部件和保存重开一致性。
- `G1`：对象身份、数量、模板版本、尺寸、贴地、初始变换、分组、朝向和明显空间冲突。
- `G2`：活动相机、画幅、分辨率、视锥、关键对象投影、关键裁切、主体占比和硬失败空白阈值。
- `G3`：关键事件帧和必要采样帧中的状态、Root/Armature/Mesh 关系、路径、设备状态、关键可见性和明显下沉或穿模。
- `G4`：帧序列或视频的数量、分辨率、帧率、时长、编码、完整解码、路径和版本对应关系。

项目第一版只需覆盖当前视频真实需要的检查，不得借机建设通用可视化后台、数据库、全自动审美评分或复杂全帧物理系统。

### 门禁边界

门禁可以证明客观条件，但不能证明动作自然、构图好看或剧情具有发布价值。

### 强制规则

1. 任一必要子检查为 `FAIL` 或 `ERROR`，技术门禁失败。
2. 项目声明为必检的子检查返回 `NOT_CHECKED` 或结果缺失，技术门禁失败，失败代码应明确为 `REQUIRED_CHECK_NOT_EXECUTED` 或等价语义。
3. run_id 不匹配、blend_path 不匹配、结果过期或结果不一致时必须阻止。
4. 不得用旧门禁结果授权新版本场景。
5. 门禁失败时不得进入相机、动画、完整预览或正式渲染等下一阶段。
6. 普通首帧构图和材质调整不默认运行完整门禁，但必须运行会被当前修改破坏的最小阶段门禁。
7. 完整预览前至少运行与当前版本直接相关的必要门禁。
8. Claude Code 必须提交原始结果、退出码、实际值、预期值和容差，不能只提交 `MET`、`PASS` 或 `TRUE`。
9. GPT、Claude Code 和用户的视觉批准都不能把机器硬失败直接改写为 `PASS`。只能修复候选，或由用户正式修改并版本化验收合同后重新运行门禁。
10. 项目门禁第一次接入，或关键目标配置发生变化时，必须使用至少一个已知错误候选或最小故障注入，证明相关错误会被门禁阻止；同时用正确候选证明该检查能够通过。该验证只覆盖本次接入的关键检查，不自动扩展成完整回归。

# P10：完整低清预览

### 前置条件

1. 世界布局和正式镜头已经批准。
2. 核心功能样片已通过。
3. 完整动画已构建。
4. 与当前版本直接相关的必要技术门禁已通过。

### 核心交付

一段完整、低分辨率、可解码的 MP4。

关键事件帧只在定位问题或核对剧情状态时按需生成；Contact Sheet 或审核板只在并列比较多个固定状态确有价值时生成，不得代替完整预览。

### 技术检查

1. 视频真实存在。
2. 非黑屏、非纯色。
3. 包含预期对象。
4. 关键动作未被裁切。
5. 视频可以完整解码。
6. 帧率、分辨率和时长符合预览要求。
7. 当前预览与当前 `.blend` 和 run_id 对应。

### GPT 视觉审核

1. 不看字幕能否理解核心因果。
2. 动作是否太快、太慢、滑行或突变。
3. 角色反应顺序是否清楚。
4. 设备状态变化是否明显。
5. 镜头是否遮挡关键事件。
6. 最终状态是否形成清楚结论。
7. 是否出现新的明显视觉错误。
8. 是否已经达到当前视频冻结的可发布级标准，或只剩不阻断发布的 `QUALITY`。

### 用户裁定

```text
APPROVED_FOR_FINAL_RENDER
REVISION_REQUIRED
STOP
```

### 修正规则

每轮只修改一个主要变量，例如：

```text
只修角色退出路径
只修某个转身时机
只修设备状态可见性
只修摄像机遮挡
```

不得在一轮同时修改角色、布局、镜头、灯光和材质。

# P11：正式渲染与输出验证

### 前置条件

1. 当前低清完整预览获得用户明确批准。
2. 必要技术门禁通过。
3. 本轮任务明确授权完整渲染。

### 正式渲染方式

默认：

```text
Blender 渲染 PNG 序列
→ 验证帧序列
→ FFmpeg 编码 MP4
→ 验证 MP4
```

不默认让 Blender 直接输出最终 MP4。

### PNG 序列最低检查

1. 文件数量等于目标帧数。
2. 首帧和末帧正确。
3. 没有缺帧。
4. 没有意外重复帧。
5. 没有损坏图片。
6. 分辨率一致且符合目标。
7. 没有意外文件混入。

### MP4 最低检查

1. 视频流存在。
2. 编码符合交付要求，默认 H.264。
3. 像素格式默认 `yuv420p`。
4. 分辨率正确。
5. 帧率正确。
6. 帧数正确。
7. 时长在容差内。
8. FFmpeg 完整解码退出码为 0。
9. 音频流数量符合当前视频设计。

当前仓库没有可直接调用的独立通用输出检查入口时，每个正式视频必须由自己的 `FORMAL_OUTPUT_VALIDATION_ENTRY` 完成全部必要检查。不能因为未来通用检查器尚未完成而跳过输出验证。

# P12：后期包装、最终审核与交付

### 后期可使用

1. 剪映。
2. jianying-mcp。
3. Remotion。
4. FFmpeg。
5. 当前项目批准的其他工具。

### 后期负责

1. 标题和字幕。
2. 箭头、高亮和辅助图形。
3. 音效和音乐。
4. 转场。
5. 封面。
6. 平台安全区。

后期只能帮助解释，不能代替 Blender 中必须真实发生的人物或设备动作。

### 最终审核

技术：

1. 最终文件和验证结果对应。
2. 编码、帧率、分辨率和时长正确。
3. 没有旧版本混入。
4. 视频可以完整播放和解码。

视觉：

1. 第一秒是否抓人。
2. 核心故事是否清楚。
3. 字幕是否遮挡主体。
4. 关键动作是否明显。
5. 结尾是否形成明确结论。
6. 用户是否愿意发布。

### 最终 delivery

只包含当前任务真正需要的文件，例如：

```text
最终 MP4
封面图
必要字幕或后期项目文件
必要的简洁验证结果
```

禁止混入：

1. 完整临时帧序列。
2. 历史失败版本。
3. 临时 `.blend`。
4. 无关测试输出。
5. 大型第三方原始资产。
6. 重复报告和无价值证据包。

## 九、固定异常排查顺序

渲染、投影或动画异常时，优先按以下顺序排查：

```text
对象身份与层级
→ 批准资产模板版本
→ 姿势与 Action
→ Root 旋转和位移
→ 可见几何世界尺寸
→ 地面接触
→ 世界位置与朝向
→ 布局合同与锚点
→ bbox 与投影输入
→ 摄像机
→ 灯光
→ 材质与后期
```

修复必须基于真实数据和可复现结果，不得只凭一张异常渲染图猜根因。

排查顺序不是机械门禁。已经有直接证据指向某一层时，可以定点检查该层，但不得借机扩大范围。

资产与布局门禁未通过前，不得进入相机盲调；相机问题不能用于掩盖资产比例、贴地或世界布局错误。

## 十、正式入口与禁止绕过

每条视频必须在 `VIDEO_PLAN.md` 中记录：

```text
FORMAL_BUILD_ENTRY
FORMAL_GATE_ENTRY
FORMAL_PREVIEW_ENTRY
FORMAL_FINAL_RENDER_ENTRY
FORMAL_OUTPUT_VALIDATION_ENTRY
```

旧项目通过这些字段映射真实入口，不强制迁移到统一模板目录。

字段指向的文件不存在、仍属实验脚本或尚未批准时，必须填写 `NOT_ASSIGNED` 或 `LEGACY_REFERENCE_ONLY`，不得猜测模板路径。

禁止：

1. 直接运行构建脚本后宣布整体完成。
2. 门禁失败后改用其他渲染脚本绕过。
3. 手动点击 Blender 渲染来绕过授权。
4. 使用旧 run_id 或旧结果文件授权新版本。
5. Claude Code 临时编写不受门禁控制的正式完整渲染脚本。
6. 只凭终端出现 `Render Saved` 就认定画面正确。
7. 用静态图片、数据采样或文字说明替代任务明确要求的视频产物。
8. 使用门禁失败或尚未批准的候选文件进入下一阶段。
9. 把必检项 `NOT_CHECKED` 当作通过。
10. 通过更换 `TASK_ID`、相机类型、脚本名或文件版本重置同一问题的止损次数。
11. 为适配镜头偷偷移动已批准世界布局。
12. 让施工 Claude Code 自行批准视觉结果。

允许用于诊断的独立最小脚本必须明确标记为诊断脚本，不得产生正式交付或被包装成正式结果。

## 十一、人工审核点

以下节点默认需要用户视觉裁定：

1. 核心资产模板的视觉比例、方向和风格。
2. 世界布局。
3. 正式首帧或镜头构图。
4. 3–5 秒核心功能样片。
5. 完整低清预览。
6. 最终成片。

其他技术选择由 GPT 决定，Claude Code 执行。

人工审核后，由 GPT 将反馈转换为一个明确的主要修改变量和可验证任务。不得把“看着不对”“继续优化”“做到满意”为原文直接发送给 Claude Code。

在正式审核前，GPT 必须先查看真实图片或视频，不得只根据 Claude Code 的终端声明向用户推荐批准。

## 十二、每轮 Claude Code 指令结构

任务提示词的长度和结构必须服从实际复杂度，不得为了形式完整机械套用大型模板。

### 简单生产任务

正式首帧、单一画面修正、单一动画片段或单文件修改等简单任务，通常只需要：

```text
PROBLEM_ID
TASK_ID
CURRENT_STAGE
UNIQUE_MAIN_GOAL
AUTHORITATIVE_INPUTS
INPUT_APPROVAL_STATUS
ALLOW_MODIFY
DO_NOT_MODIFY
MANDATORY_GATE
ACCEPTANCE_CRITERIA
STOP_CONDITIONS
DELIVERABLES
```

没有实际执行价值的背景、报告字段、验证章节和上传字段不得强行加入。

### 复杂技术任务

只有任务确实同时涉及多类输入、多个授权边界或不可分割的技术验证时，才按需增加：

```text
CURRENT_STAGE
BACKGROUND
MUST_IMPLEMENT
MUST_VERIFY
AUTHORIZED_OPERATIONS
FORBIDDEN_OPERATIONS
MUST_REPORT
MUST_UPLOAD
```

### 所有指令必须做到

1. 只有一个主要目标，最多附带一个不可分割的子目标。
2. 明确权威输入和真实路径。
3. 明确允许和禁止修改范围。
4. 明确是否授权打开 `.blend`、渲染、联网下载或完整回归。
5. 验收标准只保留决定本轮主要结果是否可用的条件。
6. 提供与真实失败原因对应的停止路径，不迫使 Claude Code 必须成功。
7. 明确最终交付物；只有用户确实需要上传文件时才要求 delivery 目录。
8. 明确不得自动进入下一阶段。
9. 汇报字段只用于陈述真实结果，不得成为实现必须满足的目标。
10. 不把 Claude Code 写成视觉批准者。
11. 只要属于同一问题的继续处理，就沿用同一 `PROBLEM_ID`。
12. 不把布局数学、质检实现和整份生产规范重复塞入每次提示词。

### 禁止开放式指令

```text
调整直到满意
看效果继续优化
根据画面自行批准
自动进入下一阶段
尝试不同参数找到最佳效果
自行决定模型比例或大量关键对象位置
顺便修复其他问题
为了通过而替换验证对象或放宽标准
```

## 十三、Claude Code 完成报告

完成报告只用于陈述本轮真实执行事实，不承担视觉批准、阶段放行或用户验收功能。

报告字段按任务需要选择，不得机械要求所有字段存在或为 `TRUE`。

### 简单视觉或单一产出任务

通常只需要：

```text
PROBLEM_ID:
TASK_ID:
TASK_STATUS:
OUTPUT_FILES:
BLOCKERS:
MACHINE_GATE_RESULT:
INDEPENDENT_VISUAL_REVIEW: PENDING_GPT_REVIEW
USER_APPROVAL: PENDING
NEXT_STAGE_STARTED: FALSE
```

### 复杂技术任务

按需增加：

```text
CURRENT_STAGE:
UNIQUE_MAIN_GOAL:
AUTHORITATIVE_INPUTS:
INPUT_APPROVAL_STATUS:
FILES_MODIFIED:
BLENDER_EXECUTED:
REAL_BLEND_OPENED:
RENDER_EXECUTED:
MACHINE_GATE_RESULT:
GATE_RESULT_FILES:
GATE_EXIT_CODE:
TESTS_OR_VALIDATIONS_RUN:
FAILED_CHECKS:
PRODUCTION_PROGRESS_THIS_ROUND:
DELIVERY_FOLDER:
DELIVERY_FILES:
DELIVERY_FILE_COUNT:
UNRELATED_FILES_IN_DELIVERY:
```

### 报告规则

1. `TASK_STATUS` 的基础定义与 `CLAUDE.md` §3 一致：`COMPLETED` 要求所有冻结验收标准均满足；`PARTIAL` 表示至少一项未满足；`BLOCKED` 表示无法满足或额度已用完。本规范进一步区分任务完成与阶段批准，`COMPLETED` 不表示视觉审核通过或候选已经晋升。
2. 视觉任务完成后必须保持 `INDEPENDENT_VISUAL_REVIEW: PENDING_GPT_REVIEW` 和 `USER_APPROVAL: PENDING`，直到对应审核真实发生。
3. 未授权的操作写 `NOT_AUTHORIZED`。
4. 未执行的操作写 `NOT_RUN`。
5. 失败写真实失败结果。
6. 不得为了让字段变成 `TRUE` 而修改实现、改变统计口径、跳过验证或使用替代产物。
7. 任务不需要用户上传文件时，不创建空 delivery 目录，也不输出无意义交付字段。

## 十四、交付规则

1. 只交付本轮用户真正需要审核或保存的文件。
2. 1–3 个文件默认直接上传。
3. 4 个及以上文件只有在保留目录结构、依赖关系或形成自包含交付确有价值时才生成 ZIP。
4. 普通任务不默认生成 Manifest、SHA256、完整报告、完整日志或证据包。
5. 临时脚本、缓存、帧序列、临时 `.blend` 和诊断文件不得混入正式 delivery。
6. 已批准或已提交审核的文件不得覆盖。
7. 最终视频生产优先于报告和打包。
8. 首帧、样片和完整预览必须直接提供真实 PNG 或 MP4，不得只交付 JSON、接触表或终端说明。

## 十五、止损规则

### 技术止损

1. 同一 `PROBLEM_ID` 一轮实现加一轮定点修正后仍未解决：停止自动修正。
2. 更换 `TASK_ID`、脚本名、文件版本或相机方案不重置次数。
3. 连续两轮没有真实生产进展：触发止损审查。
4. 连续两轮渲染验证失败：进入独立最小诊断，不重写完整构建脚本。
5. 第二轮后仍失败：如实报告 `PARTIAL` 或 `BLOCKED`，设置 `STOP_LOSS_TRIGGERED: TRUE`，决定换资产、修改前提、简化目标或暂停。

### 资产止损

1. 核心动作需要大规模拆模、重绑或重做材质：比较替换资产成本。
2. 资产能力明显不足：换资产或改剧情，不无限修资产。
3. 风格不匹配且两轮主要视觉调整无明显改善：停止该资产路线。
4. 资产技术能力已被证明后，不得为了美化诊断接触表继续返工。

### 审美止损

正式首帧、核心样片或完整低清预览经过允许的定点返工仍未达到用户接受标准，由 GPT 提出继续、换方案或停止建议，最终由用户决定。

### 流程止损

发现 GPT 提示词、Claude Code 执行或项目规则问题时：

1. 先判断是否阻断当前生产结果。
2. 只有当前 `BLOCKER` 才立即修复。
3. 一次性画面问题只修当前画面。
4. 通用规则只有经过实际生产验证后才写入长期规范。
5. 不得为寻找流程问题而暂停成片生产。

## 十六、新对话接管协议

新的 GPT 主控对话接管视频项目时，必须按顺序真实读取：

```text
1. CLAUDE.md
2. VIDEO_PRODUCTION_EXECUTION_STANDARD.md
3. 当前视频 VIDEO_PLAN.md
4. VIDEO_PLAN 指定的人工参考、批准模板、批准合同和批准场景
5. 当前正式入口脚本
6. 最近需要审核的真实交付文件
7. 当前问题的原始门禁结果
```

首次状态判断必须说明：

1. 这条视频要表达什么。
2. 当前做到哪个阶段。
3. 当前人工参考、候选和批准文件分别是什么。
4. 哪些内容已经技术通过。
5. 哪些内容已经获得用户审美批准。
6. 当前 `PROBLEM_ID`、实现次数、修正次数和止损状态。
7. 当前唯一阻断是什么。
8. 唯一下一任务是什么。

不得用历史对话记忆、Claude Code 终端声明或旧报告替代实际文件读取。

用户明确说“下一步”后，GPT 才生成一个当前阶段的 Claude Code 执行指令。不得一次生成多个阶段或自动推进。

当前任务依赖图片或视频时，必须读取或查看真实文件，不得仅引用文件名和终端声明。

## 十七、VIDEO_PLAN.md 模板

````markdown
# <VIDEO_TITLE> — VIDEO_PLAN

```text
VIDEO_ID:
VIDEO_TITLE:
PLATFORM:
ASPECT_RATIO:
ENGINEERING_VALIDATION_ASPECT_RATIO:
TARGET_RESOLUTION:
TARGET_FPS:
TARGET_DURATION:
CURRENT_STAGE:
CURRENT_PROBLEM_ID:
PROBLEM_IMPLEMENTATION_COUNT:
PROBLEM_CORRECTION_COUNT:
ACTIVE_TASK_ID:
ACTIVE_TASK_STATUS:
UNIQUE_NEXT_TASK:
LAST_TECHNICAL_GATE:
LAST_VISUAL_REVIEW:
USER_VISUAL_APPROVAL:
CURRENT_MANUAL_REFERENCE:
CURRENT_APPROVED_ASSET_TEMPLATE:
CURRENT_APPROVED_WORLD_LAYOUT:
CURRENT_APPROVED_SHOT:
CURRENT_APPROVED_BLEND:
CURRENT_APPROVED_PREVIEW:
STOP_LOSS_TRIGGERED: FALSE
FORMAL_BUILD_ENTRY:
FORMAL_GATE_ENTRY:
FORMAL_PREVIEW_ENTRY:
FORMAL_FINAL_RENDER_ENTRY:
FORMAL_OUTPUT_VALIDATION_ENTRY:
```

`ASPECT_RATIO` 表示最终发布画幅；`ENGINEERING_VALIDATION_ASPECT_RATIO` 表示单独的工程验证画幅。没有区别时填写相同值。

## 1. 视频目标

## 2. 观众最终应理解的结论

## 3. 已锁定的故事和规格

## 4. 已批准资产与许可证

## 5. 当前阶段状态

## 6. 已确认的技术事实

## 7. 已通过的技术门槛

## 8. 用户已批准的视觉版本

## 9. 当前阻断问题

## 10. 当前唯一下一任务

## 11. 推迟的 QUALITY 与 TECH_DEBT

## 12. 正式入口

## 13. 版本与审核记录
````

## 十八、当前仓库接入原则

本规范批准后，仓库应按以下顺序接入：

1. 将本文件保存为根目录 `VIDEO_PRODUCTION_EXECUTION_STANDARD.md`。
2. 根目录 `CLAUDE.md` 只引用本规范，不复制完整流程。
3. 为当前视频更新或创建真实有效的 `VIDEO_PLAN.md`。
4. 明确该项目的正式构建、门禁、预览、最终渲染和输出验证入口。
5. 识别旧项目中哪些脚本只是历史实验、哪些仍是正式入口。
6. 状态整理完成后，立即回到当前视频的直接生产任务。

不得为了接入本规范而重写已锁定的通用质检代码、重新运行无关回归或创建新的大型流程体系。

## 十九、版本修订记录

### R2 技术审核修订

R2 处理了会影响执行真实性或入口识别的问题：

1. P0 增加真实成功标记和 stdout smoke test。
2. Live GUI 改为非默认、需单独验证和明确授权的可选能力。
3. 明确动画状态、摄像机和投影检查不等于连续运动或审美验证。
4. 旧项目通过 `VIDEO_PLAN.md` 映射真实入口。
5. 建议门禁字段改为语义要求，允许旧 schema 通过映射接入。
6. 明确输出验证不能因为通用检查器未完成而跳过。
7. Claude Code 报告以真实执行结果为基础。

### R3 生产任务设计修订

R3 处理会直接影响视频生产结果、GPT 任务设计和视觉验收真实性的问题：

1. GPT 每轮先定义可直接审核、能够推进成片的主要产出。
2. 简单视觉任务不再机械套用大型提示词结构。
3. Claude Code 只报告执行事实，不拥有视觉批准或阶段放行权。
4. GPT 必须先查看实际图片或视频，再读取完成报告。
5. 接触表和技术证明不得代替正式首帧、核心样片或完整预览。
6. 质检按真实技术阻断和破坏风险介入，不默认跟随每次视觉调整。
7. 完成报告字段不得反向驱动实现或迫使结果变成 `TRUE`。
8. 首帧内部工作不再机械拆成多个没有独立决策价值的任务。

R3 未修改通用质检代码、当前视频状态、项目脚本或 `CLAUDE.md`。

### R4 生产基准与强制门禁修订

R4 根据真实视频生产中已经发生并反复确认的问题，补齐以下流程缺口：

1. 增加 `MANUAL_REFERENCE / CANDIDATE / APPROVED` 产物身份。
2. 明确批准文件只读，失败或未批准候选不得进入下一阶段。
3. 增加核心资产世界尺寸、最低点、方向、统一父级、锚点和保存重开要求。
4. 禁止 Claude Code 在已有批准模板时重复导入原始资产并自行猜测比例。
5. 增加批准布局合同和确定性布局要求，减少大量关键对象自由坐标。
6. 区分 `WORLD_LAYOUT_APPROVED` 与 `SHOT_COMPOSITION_APPROVED`。
7. 增加 G0–G4 阶段门禁模型，并明确只运行当前项目真实需要的最小检查。
8. 明确必检项 `NOT_CHECKED`、结果缺失、旧结果和 run_id 不匹配均导致门禁失败。
9. 强制关键 `.blend` 保存后由全新 Blender 进程重新打开并验证。
10. 区分机器门禁、GPT 独立视觉审核和用户批准。
11. 将返工上限改为按 `PROBLEM_ID` 统计，禁止通过更换任务名或方案名清零。
12. 允许明确区分工程验证画幅与最终发布画幅。
13. 基础流程发生重大修正后，优先使用真实资产完成最小端到端样片。
14. 保留生产优先和最小验证原则，不要求重写通用质检系统、建设大型平台或运行无关完整回归。
15. 明确新门禁接入必须用一个已知错误候选或最小故障注入证明阻断真实生效。
16. 明确机器硬失败不能被人工直接改写为通过，只能修复或正式版本化修改合同后重检。
17. 限制单轮相机候选数量，并区分世界布局诊断视图与正式镜头。
18. 修正 P0 正式入口要求，未进入阶段的入口允许为 `NOT_ASSIGNED`，只阻断当前任务真实依赖的入口。
19. 补齐总生产架构中的核心功能样片、完整动画配置、确定性构建和动画门禁阶段。
20. 区分首次首帧合并构建与后续单变量定点修正。
21. 明确相机实现轮最多三个候选，修正轮只能修改其中一个候选，不得创建第四种方案。
22. 明确解除当前 `BLOCKER` 所必需的资产测量、门禁接入和故障验证可以成为独立技术任务。
23. 文档在用户最终确认前保持 `PENDING_USER_APPROVAL`。

R4 只修改通用生产规范，不自动修改任何当前视频状态、`.blend`、资产、项目脚本、`VIDEO_PLAN.md` 或通用质检实现。

## 二十、最终原则

以后所有视频遵守下面这句话：

> 用户批准视觉答案，批准模板、合同和场景保存答案，确定性脚本执行答案，项目门禁阻止答案被改坏，GPT 控制阶段与止损，Claude Code 只在授权边界内施工。质检服务于生产，但错误没有被配置、执行和阻断时，检查器存在本身没有意义。最终目标始终是完成并交付视频。
