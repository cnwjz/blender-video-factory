# Blender 资产驱动小实验交接文档 v1

> 交接日期：2026-07-14  
> 用途：供新的 GPT 对话与 Claude Code 会话直接接手 Blender 资产驱动对照实验。  
> 当前状态：上一轮 Blender 原生几何体路线已完成技术验证并暂停。  
> 本轮目标：验证“成熟统一资产 + 明确视觉参考 + Claude Code 负责摆放、动画与渲染”能否在可接受成本内达到抖音发布级视觉门槛。  
> 工作目录：`D:\blender-video-factory`

---

## 一、本轮实验的核心结论

上一轮 Blender 测试已经证明：

1. Blender 5.1.2、Eevee、命令行渲染、PNG 序列和 FFmpeg 编码全部可用。
2. Claude Code 可以通过确定性 `bpy` 脚本稳定构建场景。
3. Character Root 层级可以稳定承载人物整体移动。
4. `world_to_camera_view` 可以完成摄像机构图预检。
5. Blender 能够清楚表达排队、分流、窗口关闭和拥堵等空间机制。
6. 灰盒阶段的静音因果表达已经通过人工审核。
7. 失败集中在发布级美术质量、人物造型、环境识别、光影设计和返工成本。
8. 原生立方体、圆柱和球体很容易形成“程序化低多边形练习”观感。
9. Claude Code 同时承担导演、建模、美术、灯光、动画和工程实现时，容易优先满足坐标、帧数和对象数量，难以稳定达到成熟视觉质量。
10. 上一轮不能证明 Blender 工具本身无法制作发布级视频，只能证明旧生产方法无法在可接受成本内达到目标。

因此，本轮实验不继续旧项目的 `style_v5`，也不继续让 Claude Code 从空场景设计全部美术。

新的方法固定为：

```text
成熟统一资产
+ 明确视觉参考
+ 预制材质与灯光模板
+ Claude Code 负责导入、摆放、动作、摄像机和渲染
+ 人工分阶段审核
```

---

## 二、本轮实验要回答的问题

本轮只回答一个问题：

> 在使用成熟资产、明确视觉参考和固定模板后，Claude Code 能否把 Blender 画面推进到明显高于上一轮 V4，并具备抖音发布级潜力？

本轮不回答：

- 能否批量生产几十条视频
- 能否完全自动选题
- 能否完全自动下载资产
- 能否自动生成完整剪映工程
- 能否制作复杂人物剧情
- 能否建立完整 Blender 视频工厂

只有首帧与短动作同时通过后，才讨论后续生产化。

---

## 三、实验名称与范围

项目 ID：

```text
bvf_asset_test_001_checkout_lane
```

建议项目目录：

```text
D:\blender-video-factory\projects\bvf_asset_test_001_checkout_lane
```

实验内容：

> 一个收银窗口关闭后，三至五名顾客向旁边队伍移动。

目标时长：

```text
4 至 6 秒
```

目标平台：

```text
抖音
```

输出规格：

```text
1080×1920
30fps
Eevee
PNG 序列优先
```

实验范围：

- 一个超市收银区局部
- 两个或三个收银通道
- 三至五名顾客
- 一名或两名收银员
- 一个窗口关闭动作
- 一个清楚的横向分流动作
- 一台静止或极轻微运动的摄像机
- 一个经过设计的首帧

实验不追求完整机制闭环，不重复制作上一条 11.5 秒视频。

---

## 四、本轮与上一轮的根本区别

上一轮：

```text
Claude Code
→ 从空场景创建原生几何体
→ 自己设计人物、柜台、材质、灯光
→ 编写完整 bpy 脚本
→ 连续四轮风格返工
```

本轮：

```text
人工与 GPT 锁定视觉参考
→ 选择一套统一风格资产
→ 建立首帧 lookdev 场景
→ Claude Code 只负责导入、摆放、动作和渲染
→ 首帧通过后才进入动画
```

本轮强制分工：

| 工作 | 主要负责人 |
|---|---|
| 风格方向 | GPT + 用户 |
| 参考图锁定 | 用户人工确认 |
| 资产许可审核 | Claude Code 执行，GPT 审核 |
| 资产导入与整理 | Claude Code |
| 场景摆放 | Claude Code |
| 摄像机与构图 | Claude Code + 自动预检 |
| 灯光模板 | 现成模板或一次性人工建立 |
| 角色动作 | Claude Code |
| 发布价值判断 | 用户人工审核 |
| 最终后期 | 剪映专业版 |

Claude Code 不再承担从零发明整体美术风格。

---

## 五、上一轮可直接复用的能力

以下内容直接复用，不重复开发。

### 1. 环境与渲染

- Blender 5.1.2
- `blender --background`
- Eevee
- PNG 序列输出
- FFmpeg PNG 转 MP4
- 帧提取与 contact sheet
- Windows 10 路径处理
- Blender 5.1 API 兼容经验

### 2. 角色与动画

- Character Root Empty 层级
- 所有位置动画写在 Root 上
- 模型网格只作为 Root 子对象
- 三段式移动路径
- `AUTO_CLAMPED` 或无超调插值
- 角色整体隐藏规则
- 角色阴影同步关闭

### 3. 摄像机和构图

- `world_to_camera_view`
- 关键帧投影边界检查
- 跨帧联合包围盒
- 正交摄像机构图经验
- 顶部空白、左右边距、底部边距自动检查
- 关键对象裁切检测

### 4. 工程与审核

- 确定性配置 JSON
- 构建脚本版本化
- 输出不覆盖旧版本
- `UPLOAD_NEXT` 统一审核目录
- 2×2 review board
- 完整 MP4 + dense contact sheet
- 用户人工锁定

---

## 六、必须避免重复的坑

### 1. 禁止从空场景设计全部美术

禁止继续使用：

- 原生球体做人头
- 原生圆柱做完整人物
- 原生立方体直接拼完整场景
- 仅靠 RGB 数值猜测发布级配色
- 仅靠一句“低多边形微缩模型”定义风格

基础几何体只允许用于遮挡测试、碰撞代理和临时辅助。

### 2. 禁止同时解决全部问题

首帧阶段只审核：

- 构图
- 人物资产质量
- 场景资产质量
- 材质统一
- 光影
- 超市识别
- 第一眼吸引力

动画阶段才审核：

- 动作路径
- 穿模
- 速度
- 节奏
- 关闭动作

### 3. 禁止继承弱视觉方向反复打补丁

如果首帧基础设计较弱，直接更换资产组合或视觉参考。

不允许连续出现：

```text
V1 修构图
V2 修人物
V3 修颜色
V4 修收银员
V5 继续补丁
```

首帧最多两轮主要返工。

### 4. 禁止没有视觉参考就开始摆场景

在任何 Blender 构建前，必须锁定：

- 人物比例参考
- 场景构图参考
- 灯光参考
- 材质参考
- 色彩参考

### 5. 禁止混用多个不一致的资产包

人物、柜台、商品、货架和环境必须满足：

- 轮廓语言接近
- 面数和细节等级接近
- 材质粗糙度接近
- 色彩饱和度接近
- 人物比例一致

### 6. 禁止让自动审核替代人工判断

自动审核可以检查：

- 对象是否存在
- 是否裁切
- 是否穿模
- 亮度是否异常
- 角色是否完整
- 中间窗口是否关闭

自动审核不能决定：

- 第一眼是否吸引人
- 是否像抖音内容
- 是否像成熟三维作品
- 是否值得继续投入

---

## 七、资产要求

本轮允许并鼓励使用外部成熟资产。

### 1. 人物资产

必须满足：

- 风格化或低多边形
- 统一人物比例
- 无写实人脸
- 角色轮廓清楚
- 最好具备基础骨骼
- 至少包含站立姿势
- 最好包含简单走路循环
- 商业使用许可清楚
- 可在 Blender 5.1 正常导入
- 可修改材质
- 不依赖在线服务才能打开

人物数量只需：

```text
顾客 3 至 5 个
收银员 1 至 2 个
```

允许使用同一人物资产做服装或材质变体。

### 2. 收银区资产

必须至少包含：

- 收银台
- 传送带
- 扫描区
- 灯牌或状态灯
- 可关闭的挡板、门或窗口部件
- 少量商品
- 可选购物篮或购物车

收银台必须在首帧中一眼可识别。

### 3. 环境资产

最多使用：

- 一组地面
- 一面背景墙
- 两组货架局部
- 少量商品陈列

环境只负责强化超市语义。

### 4. 材质和灯光

优先使用：

- 资产包原生材质
- 同一套 PBR 或风格化材质
- 经过验证的 Eevee 灯光模板
- 柔和主光
- 前方补光
- 轻微轮廓光
- 适度环境遮蔽

禁止混用写实 PBR 人物与卡通收银台。

---

## 八、资产许可规则

每个外部资产必须记录：

- 资产名称
- 来源页面
- 作者或发布者
- 下载日期
- 许可证名称
- 是否允许商业使用
- 是否允许修改
- 是否要求署名
- 是否允许再分发
- 原始文件路径
- Blender 导入文件路径
- 项目内修改说明

记录文件：

```text
projects\bvf_asset_test_001_checkout_lane\assets\ASSET_MANIFEST.md
```

许可证副本或截图放入：

```text
projects\bvf_asset_test_001_checkout_lane\assets\licenses
```

禁止：

- 使用来源不明模型
- 使用无法确认商业许可的素材
- 把付费资产文件上传给 GPT
- 在交接包中分发第三方原始资产
- 使用明显来自影视、游戏或品牌 IP 的模型
- 使用带有品牌商标的超市资产

---

## 九、视觉参考要求

正式构建前需要准备 3 至 5 张参考图。

### 参考 A：整体构图

- 竖屏
- 主体占画面大部分区域
- 能看到柜台和排队人物
- 顶部空白有限
- 具备明显前景、中景和背景关系

### 参考 B：人物比例

- 人物头身比例清楚
- 轮廓易读
- 动作姿态简单
- 远景仍能辨认四肢和朝向

### 参考 C：材质与光影

- 柔和光影
- 人物和地面分离
- 阴影集中在脚下
- 柜台边缘有层次
- 背景低对比

### 参考 D：配色

- 主色不超过三组
- 明确强调色
- 人物服装有少量变化
- 无杂乱高饱和色

### 参考 E：超市识别

- 收银台、传送带和商品清楚
- 不依赖文字也能认出超市结账场景

参考图锁定后，创建：

```text
projects\bvf_asset_test_001_checkout_lane\REFERENCE_LOCK.md
```

文件需要记录：

- 每张参考图承担什么作用
- 哪些元素必须复刻
- 哪些元素禁止复刻
- 允许的偏差
- 用户人工锁定结论

在用户锁定前，Claude Code 不得开始正式首帧构建。

---

## 十、实验阶段

### 阶段 A0：资产候选审计

目标：找到少量风格一致的资产候选。

输出：

```text
ASSET_CANDIDATES.md
ASSET_MANIFEST_DRAFT.md
```

只做：

- 本地资产检查
- 格式检查
- 许可检查
- Blender 导入兼容性检查
- 风格一致性截图
- 面数与材质数量检查

不做完整场景。

验收：

- 至少一套人物候选
- 至少一套收银区候选
- 许可证清楚
- 风格一致
- Eevee 可用
- 无明显缺失贴图

### 阶段 A1：资产导入冒烟测试

输出一张测试图，画面只包含：

- 一个顾客
- 一个收银员
- 一个收银台
- 一个商品
- 一块地面
- 基础灯光

检查：

- 模型比例
- 材质
- 法线
- 贴图路径
- 阴影
- 骨骼
- Blender 5.1 兼容性

### 阶段 A2：首帧 lookdev

只制作 F001 首帧，不制作动画。

首帧必须包含：

- 一个清楚的超市收银场景
- 两个或三个收银通道
- 三至五名顾客
- 一至两名收银员
- 清楚的传送带和商品
- 清楚的空间层次
- 明确的主视觉焦点
- 与参考图一致的材质和灯光

输出：

```text
F001_lookdev_v1.png
review_board_lookdev_v1.png
```

最多两轮主要返工。

### 阶段 A3：关键状态静帧

首帧通过后，制作：

```text
F001 正常
F060 关闭开始
F090 关闭完成
F150 分流中
```

输出：

```text
review_board_keyframes_v1.png
```

### 阶段 A4：4 至 6 秒动作预览

静帧通过后，制作完整动作。

允许：

- 简单走路循环
- Root 位移
- 窗口关闭
- 灯光状态变化
- 极轻微摄像机运动

输出：

```text
preview_v1.mp4
contact_sheet_dense_v1.png
```

### 阶段 A5：最终判断

最终判断只允许两种：

```text
asset_driven_blender_test: passed
```

或：

```text
asset_driven_blender_test: failed
```

---

## 十一、首帧验收标准

### 场景识别

1. 第一眼认出超市收银区。
2. 传送带、扫描器和商品可辨认。
3. 收银员位置正确。
4. 顾客排队方向清楚。
5. 不依赖文字解释场景。

### 构图

1. 竖屏主体占据画面主要区域。
2. 顶部无意义空白不超过 15%。
3. 左右安全边距不低于 4%。
4. 人物完整显示。
5. 收银台没有裁切。
6. 画面有明确焦点。
7. 前景、中景、背景层次清楚。

### 人物与资产

1. 人物资产有成熟造型。
2. 人物比例统一。
3. 收银员与顾客能区分。
4. 人物没有穿模。
5. 人物脚底接触地面。
6. 材质没有丢失。
7. 模型风格统一。

### 灯光与材质

1. 人物和背景分离。
2. 阴影不过黑。
3. 阴影集中在脚下。
4. 柜台结构有清楚明暗。
5. 背景不抢主体。
6. 强调色使用明确。
7. 没有默认灰盒感。

### 发布潜力

1. 第一帧明显高于上一轮 `review_board_style_v4.png`。
2. 不像学校三维作业。
3. 不像素材随意拼装。
4. 用户本人愿意继续看下一秒。
5. 用户愿意把这套视觉继续发展为成片。

任何一项核心发布潜力未通过，禁止进入动画。

---

## 十二、关键帧验收标准

F001：

- 正常收银状态
- 人物和柜台位置正确
- 超市语义成立

F060：

- 关闭动作开始
- 中间窗口成为视觉焦点
- 顾客仍然完整

F090：

- 窗口关闭完成
- 收银员处理正确
- 关闭状态无需文字即可理解

F150：

- 至少一名顾客横向移动
- 分流方向清楚
- 资产没有穿模
- 动作没有破坏首帧美术质量

---

## 十三、动画验收标准

完整 4 至 6 秒预览必须满足：

1. 关闭动作清楚。
2. 顾客分流方向清楚。
3. 人物移动没有滑冰感或明显漂浮。
4. 有走路循环时，脚步与位移基本匹配。
5. 没有角色互相穿模。
6. 没有柜台穿模。
7. 没有材质闪烁。
8. 没有贴图丢失。
9. 没有黑帧和缺帧。
10. 摄像机运动不会眩晕。
11. 第一秒依然有观看吸引力。
12. 视觉质量没有因为动画而明显下降。

---

## 十四、返工限制

首帧阶段最多两轮主要返工。

主要返工包括：

- 更换人物资产
- 更换收银台资产
- 更换构图
- 更换灯光方向
- 更换整体配色

关键帧阶段最多两轮主要返工。

动画阶段最多一轮主要动作返工。

超过限制后触发止损。

---

## 十五、止损标准

出现以下任意情况，应结束资产驱动实验：

1. 无法找到许可证清楚且风格统一的资产组合。
2. 资产导入后出现大量缺失贴图、法线或兼容问题。
3. 首帧两轮主要返工后仍像素材拼装。
4. 首帧仍没有明显高于上一轮 V4。
5. 第一帧没有抖音观看吸引力。
6. 人物和场景资产风格无法统一。
7. 为一个 4 至 6 秒镜头需要大量手工重建资产。
8. Claude Code 无法稳定导入、重开和复现项目。
9. 动画加入后画面质量明显下降。
10. 用户本人不愿意继续观看。
11. 资产成本、授权成本或人工成本超过可接受范围。
12. 需要继续第三轮主要美术返工。

止损后保留：

- 资产清单
- 许可记录
- 导入脚本
- 灯光模板
- 摄像机模板
- 首帧
- 关键帧
- 审核记录

---

## 十六、项目目录建议

只创建当前实验需要的结构：

```text
projects/
└── bvf_asset_test_001_checkout_lane/
    ├── HANDOFF_ASSET_TEST_v1.md
    ├── INPUT.yaml
    ├── REFERENCE_LOCK.md
    ├── ASSET_CANDIDATES.md
    ├── assets/
    │   ├── imported/
    │   ├── converted/
    │   └── licenses/
    ├── scene/
    │   ├── asset_smoke_test.blend
    │   ├── lookdev_v1.blend
    │   └── animation_v1.blend
    ├── scripts/
    │   ├── audit_assets.py
    │   ├── import_assets.py
    │   ├── build_lookdev.py
    │   ├── animate_test.py
    │   └── render_review.py
    ├── reviews/
    │   ├── asset_smoke/
    │   ├── lookdev/
    │   ├── keyframes/
    │   ├── motion/
    │   └── UPLOAD_NEXT/
    └── reports/
        ├── ASSET_AUDIT.md
        ├── LOOKDEV_REPORT.md
        └── MOTION_REPORT.md
```

禁止为了结构完整创建大量空文件。

---

## 十七、统一上传规则

所有需要用户上传给 GPT 的文件统一放入：

```text
projects\bvf_asset_test_001_checkout_lane\reviews\UPLOAD_NEXT
```

每次执行前先清空该目录。

资产候选阶段只放：

```text
asset_candidate_board.png
ASSET_CANDIDATES.md
```

冒烟测试阶段只放：

```text
asset_smoke_board.png
```

首帧阶段只放：

```text
review_board_lookdev_v1.png
```

关键帧阶段只放：

```text
review_board_keyframes_v1.png
```

动作阶段只放：

```text
preview_v1.mp4
contact_sheet_dense_v1.png
```

用户只需上传 `UPLOAD_NEXT` 中全部文件。

---

## 十八、状态机

```text
planned
asset_auditing
asset_candidates_ready
asset_smoke_testing
reference_locked
lookdev_building
lookdev_reviewing
lookdev_passed
keyframes_building
keyframes_reviewing
keyframes_passed
motion_rendering
motion_reviewing
passed
failed
archived
```

---

## 十九、Claude Code 首次任务

首次任务只进行资产候选审计和实验目录初始化。

可以直接发送：

```text
请在 D:\blender-video-factory 开始 Blender 资产驱动小实验。

项目 ID：

bvf_asset_test_001_checkout_lane

请先完整读取：

1. Blender 资产驱动小实验交接文档 v1
2. FINAL_VALIDATION_REPORT.md
3. REUSABLE_RESULTS.md

本轮只执行阶段 A0：资产候选审计。

目标：

为一个 4 至 6 秒的超市收银窗口关闭镜头，找到可以用于 Blender 5.1、Eevee 和商业短视频的统一风格资产候选。

本轮禁止开始完整场景，禁止开始动画，禁止下载大型资产包，禁止修改旧项目。

请完成：

1. 创建项目最小目录。
2. 检查 D:\blender-video-factory 和用户指定目录中是否已经存在可用人物、收银台、商品、货架、材质或灯光资产。
3. 列出每个候选资产的名称、格式、文件大小、面数、材质数量、贴图数量、骨骼状态和 Blender 5.1 兼容性。
4. 检查许可证和商业使用条件。
5. 许可证不明确的候选直接标记为 rejected。
6. 检查人物、收银台和环境资产是否属于统一风格。
7. 对候选资产进行最小导入测试，禁止搭完整场景。
8. 每个候选只渲染一张简单白底或中性背景缩略图。
9. 制作 asset_candidate_board.png。
10. 创建 ASSET_CANDIDATES.md。
11. 创建 ASSET_MANIFEST_DRAFT.md。
12. 创建 ASSET_AUDIT.md。
13. 清空 reviews\UPLOAD_NEXT。
14. 只复制以下文件到 UPLOAD_NEXT：
    asset_candidate_board.png
    ASSET_CANDIDATES.md

ASSET_CANDIDATES.md 必须明确给出：

1. 推荐人物候选。
2. 推荐收银区候选。
3. 推荐环境候选。
4. 推荐材质或灯光候选。
5. 每个候选的许可证状态。
6. 每个候选的风格一致性判断。
7. 缺失的资产类型。
8. 是否满足进入 A1 冒烟测试的条件。

不得自动选择并下载付费资产。
不得自动接受需要署名但未记录的资产。
不得混用明显不同风格的候选。
不得创建完整收银场景。
不得进入 A1。
不得自动开始动画。

完成后停止，等待用户和 GPT 审核。
```

---

## 二十、GPT 在本轮中的职责

GPT 负责：

1. 审核资产候选板。
2. 判断风格是否统一。
3. 审核许可信息是否完整。
4. 和用户一起锁定视觉参考。
5. 首帧阶段重点判断发布潜力。
6. 阻止 Claude Code 在首帧未通过时进入动画。
7. 控制返工轮数。
8. 到达止损条件时立即结束实验。

GPT 不负责：

- 替用户购买资产
- 接收或分发付费资产文件
- 仅凭自动评分宣布首帧通过
- 在没有参考图时让 Claude Code自由设计
- 把实验扩展成完整工厂建设

---

## 二十一、当前唯一下一步

当前只做：

```text
创建新实验项目
→ 审计本地已有资产
→ 形成少量候选
→ 审核许可和风格
→ 输出统一候选板
```

在资产候选和视觉参考锁定前：

- 不开始完整场景
- 不制作动画
- 不渲染完整视频
- 不建设完整工厂
- 不继续旧项目 style_v5
- 不修改 D:\video-factory
